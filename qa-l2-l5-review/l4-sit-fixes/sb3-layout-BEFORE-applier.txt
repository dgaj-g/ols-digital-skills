qa-sb3-layout: 10 FAILURE(S)
  ✗ maze-escape-starter.sb3 · Beetle: TOO TIGHT — event_whenflagclicked @(53,60) and event_whenkeypressed [right arrow] @(53,240) clear each other by only 44px (need 80)
  ✗ maze-escape-starter.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [right arrow] @(53,240) and event_whenkeypressed [left arrow] @(53,420) clear each other by only 44px (need 80)
  ✗ maze-escape-starter.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [left arrow] @(53,420) and event_whenkeypressed [up arrow] @(53,600) clear each other by only 44px (need 80)
  ✗ maze-escape-starter.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [up arrow] @(53,600) and event_whenkeypressed [down arrow] @(53,780) clear each other by only 44px (need 80)
  ✗ quiz-master-starter.sb3 · Abby: TOO TIGHT — event_whenflagclicked @(53,60) and event_whenbroadcastreceived @(53,320) clear each other by only 28px (need 80)
  ✗ maze-escape-complete.sb3 · Beetle: OVERLAP — event_whenflagclicked @(53,60) intersects event_whenkeypressed [right arrow] @(53,240) (x by 280px, y by 4px)
  ✗ maze-escape-complete.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [right arrow] @(53,240) and event_whenkeypressed [left arrow] @(53,420) clear each other by only 44px (need 80)
  ✗ maze-escape-complete.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [left arrow] @(53,420) and event_whenkeypressed [up arrow] @(53,600) clear each other by only 44px (need 80)
  ✗ maze-escape-complete.sb3 · Beetle: TOO TIGHT — event_whenkeypressed [up arrow] @(53,600) and event_whenkeypressed [down arrow] @(53,780) clear each other by only 44px (need 80)
  ✗ quiz-master-complete.sb3 · Abby: OVERLAP — event_whenflagclicked @(53,60) intersects event_whenbroadcastreceived @(53,320) (x by 280px, y by 20px)
