# CUT_SCOPE.txt — what THIS cut may touch (leash L2; read by qa-scope.js).
# One path or glob per line, relative to maths/mathshelf. `*` matches anything,
# `/` included. A file changed since the cut's start commit (tools/qa/out/
# cut-start.json) that matches no line here fails qa-scope by name.
#
# HOTFIX, 12 Sept 2026 15:xx (Fable): a slow store is not a broken one — the
# 'relay-failed' sentence, one quiet retry on the passcode check, the staff
# waiting line at a size he can see; then the same for the teacher's LANDING
# (script.js staffCover) — the screen he was looking at. 60-minute clock. Client only.
strings.js
staff.js
script.js
style.css
tools/qa/CUT_SCOPE.txt
tools/qa/MATHS_*.md
tools/qa/out/*
server/Index.html
server/DEPLOY_LOG.md
HANDOVER.md
PROGRESS.md
