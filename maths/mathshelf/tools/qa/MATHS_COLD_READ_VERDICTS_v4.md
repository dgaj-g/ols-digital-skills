TRANSCRIPT HASH: f2e235f1c53605e1

I judged all 288 lines in `_v4.md`, one row per key, in the order they're listed, reading each aloud cold against `COLD_READ_CHECKLIST.md` alone — no other file, no source, no design pack. Lines under `strings.js > pupil > *` and the `stats-quartiles > *` movie captions and question prompts were read from the pupil's seat — an eleven- or twelve-year-old, regardless of which book is actually open — using the reader-and-screen rules, the five per-sentence questions, the must-fail/must-pass calibration, and the shape rules. Lines under `strings.js > teacher > *` were read from the teacher's seat, using the §6 register rules. I gave the closest reading to material this file's own annotations mark as new since the last pass — the `statPill*` stage-strip names and `statStage*` full instructions (neither key family appears anywhere in the previous `_v4.md` verdict at all, so this is their first cold read), the `✓`-prefixed commit buttons, the shelf line about more books coming, and the teacher's tick and Set-up lines. The default was FAIL throughout: a line only passed if it read clean on a first, cold pass, with no credit for being merely grammatical.

## The sentence table

| verdict | where it is | the sentence | why |
|---|---|---|---|
| PASS | strings.js > pupil > coverBusy | Opening your books… | Says plainly what it's waiting for. |
| PASS | strings.js > pupil > coverGetting | Getting your details… | Clear, one named wait. |
| PASS | strings.js > pupil > coverWelcome | Welcome | One plain word. |
| PASS | strings.js > pupil > coverOpen | Open your books | Concrete noun, concrete action. |
| PASS | strings.js > pupil > coverSwitch | Not you? Switch account | Reads exactly as a person would say it. |
| PASS | strings.js > pupil > coverFirstTime | First time here? Google will ask your permission once, then you are straight in. | Tells her exactly what happens and that it's a one-off. |
| PASS | strings.js > pupil > coverNoServer | We could not reach the server. Check your connection and reload the page. | Blames her connection, not "the wifi"; tells her what to do. |
| PASS | strings.js > pupil > coverWrongClass | That class link is not active. Ask your teacher for the link again. | Clear reason, clear next step. |
| PASS | strings.js > pupil > coverNameLabel | Your name | Plain label. |
| PASS | strings.js > pupil > coverNamePrompt | We could not read your name from your account. Write it once and it is saved. | Clear, and says it's only once. |
| PASS | strings.js > pupil > coverNameMissing | Write your name first, so your teacher sees it on her class list. | Reason given with the instruction. |
| PASS | strings.js > pupil > coverPreview | This is the preview copy of MathShelf. Nothing here is saved to school. | "This" points at the screen in front of her; the second sentence spells out what "preview" means in practice. |
| PASS | strings.js > pupil > shelfGreetingMorning | Good morning | Fine as is. |
| PASS | strings.js > pupil > shelfGreetingAfternoon | Good afternoon | Fine as is. |
| PASS | strings.js > pupil > shelfPlain | Your maths books live here. Your teacher chooses which ones are out. | The checklist's own approved shelf line, unchanged. |
| PASS | strings.js > pupil > shelfMore | Your teacher decides which books are on this shelf, and may add more during the year. | Fixed. No longer an unconditional promise — true whether or not every book currently built happens to be out for this class already. |
| PASS | strings.js > pupil > shelfMarks | 6 of 8 marks · 5 of 8 answered | A plain scorecard in ordinary school words. |
| PASS | strings.js > pupil > backToShelf | The shelf | Plain nav label, a noun she already knows. |
| PASS | strings.js > pupil > opening | Opening Angles… | Names the exact book. |
| PASS | strings.js > pupil > nudgeBanner | Your teacher suggested watching this method. | The checklist's own approved line, unchanged. |
| PASS | strings.js > pupil > selfEvalSaved | Saved. Your teacher sees this on her class list. | Clear. |
| PASS | strings.js > pupil > selfEvalHeading | Exercise finished — how did that go? | Plain, friendly, she knows exactly what just happened. |
| PASS | strings.js > pupil > selfEvalOptional | optional | Plain label. |
| PASS | strings.js > pupil > saveWaiting | Still saving your work… | Says what it's waiting for. |
| PASS | strings.js > pupil > saveRetry | Try again | Plain button. |
| PASS | strings.js > pupil > saveHeldLocal | Your work is safe on this device and will be sent as soon as the page can reach the server. | Reassuring, exact, blames nothing. |
| PASS | strings.js > pupil > statCheckCftable | Mark my table | Fits what she built. |
| PASS | strings.js > pupil > statCheckCfplot | Mark my points and curve | Fits a plotting task, not generic "working." |
| PASS | strings.js > pupil > statCheckCfread | Mark my readings | Fits a reading-off-the-graph task. |
| PASS | strings.js > pupil > statCheckBoxplot | Mark my box plot | Fits what she drew. |
| PASS | strings.js > pupil > statCheckCompare | Mark my comparison | Fits the sentence-building task. |
| PASS | strings.js > pupil > statCheckJudge | Mark my answers | Fits a judging task. |
| PASS | strings.js > pupil > statCheckValues | Mark my answers | Fits a values-entry task; reused label is fine since each only ever appears on its own screen. |
| PASS | strings.js > pupil > statAlreadyMarked | This question is already marked. | "This question" is plainly the one in front of her. |
| PASS | strings.js > pupil > statTryAgain | Not quite. Your first go stays above — one more attempt. | Reads as a person would say it; tells her what she'll see and what's left. |
| PASS | strings.js > pupil > statPutBack | Press it again to put it back. | Device-neutral verb, "it" is whatever she just pressed. |
| PASS | strings.js > pupil > statRemoveLast | Remove the last one | Plain button beside the thing it removes. |
| PASS | strings.js > pupil > statNextRow | next ↓ | Clear with the arrow. |
| PASS | strings.js > pupil > statThatsMine | ✓ That's my answer | The tick reads as a universal confirm icon, not a word to sound out; the words beside it are plain. |
| PASS | strings.js > pupil > statThatsMyOne | ✓ That's my median | Same reasoning; "median" is this exercise's own subject. |
| PASS | strings.js > pupil > statRuleFiction | The rule is the line that crosses the graph. Move it up the frequency axis to the height you need, then read where it meets the curve. | Defines "the rule" in plain words before asking her to use it; "Move" is device-neutral. |
| PASS | strings.js > pupil > statTrayFiction | Five markers — put each one on the scale where it belongs. | The checklist's own approved line for this fiction, word for word. |
| PASS | strings.js > pupil > statMedian | median | The book's own subject word, as a label. |
| PASS | strings.js > pupil > statLowerQuartile | lower quartile | Core term, as a label. |
| PASS | strings.js > pupil > statUpperQuartile | upper quartile | Core term, as a label. |
| PASS | strings.js > pupil > statIqr | interquartile range | Core term, spelled out in full — contrast the bare "IQR" pill flagged below. |
| PASS | strings.js > pupil > statRange | range | Core term and an everyday word too. |
| PASS | strings.js > pupil > statLowest | Lowest | Plain label. |
| PASS | strings.js > pupil > statHighest | Highest | Plain label. |
| PASS | strings.js > pupil > statClassColumn | Group | Sensibly avoids the confusing word "class" for a table heading. |
| PASS | strings.js > pupil > statFrequency | Frequency | Plain heading, core term. |
| PASS | strings.js > pupil > statCumulativeFrequency | Cumulative frequency | Full term as a heading. |
| PASS | strings.js > pupil > statReading | reading | Plain label. |
| PASS | strings.js > pupil > statQlistOrderWhy | Put every value in the row first. | Clear. |
| PASS | strings.js > pupil > statQlistPickWhy | Choose each value you were asked for first. | Clear. |
| PASS | strings.js > pupil > statQlistCommit | ✓ That's my median | Same commit label as above, fine here too. |
| PASS | strings.js > pupil > statQlistCommitWhy | Choose a value in the row first. | Clear. |
| PASS | strings.js > pupil > statQlistIqrWhy | Work out the interquartile range first. | Clear, uses the full term. |
| PASS | strings.js > pupil > statQlistTruth | In order: 4, 7, 7, 9, 12, 15, 21 | True answer, shown only once attempts are used up. |
| PASS | strings.js > pupil > statPickOne | median = 65 | A plain readout of her own working. |
| PASS | strings.js > pupil > statPickPair | median = (9 + 12) ÷ 2 = 65 | Plain readout. |
| PASS | strings.js > pupil > statLineOne | median = 65 | Same readout, fine here too. |
| PASS | strings.js > pupil > statLinePair | median = (9 + 12) ÷ 2 = 65 | Same readout, fine here too. |
| PASS | strings.js > pupil > statCftableRow | Cumulative frequency up to 65 minutes | Real term, real number, real unit. |
| PASS | strings.js > pupil > statCftableWhy | Put a running total in at least one row first. | Reads clean aloud; "running total" already carries its own real name from other lines. |
| PASS | strings.js > pupil > statCftableTruth | Running totals: 4, 7, 7, 9, 12, 15, 21 | True answer, shown only after attempts are used up. |
| PASS | strings.js > pupil > statPlotPlaceWhy | Place every point first. | Clear. |
| PASS | strings.js > pupil > statPlotJoinWhy | Join the points first. | Clear. |
| PASS | strings.js > pupil > statJoinPoints | ✓ Join the points | Tick as icon, plain verb-first label. |
| PASS | strings.js > pupil > statPlotJoinedAlready | The points are joined. | Plain status. |
| PASS | strings.js > pupil > statPlotEnough | You have all the points you need — move one instead. | Tells her exactly what to do instead. |
| PASS | strings.js > pupil > statPointReadout | (30, 18) | A coordinate readout of a point she placed herself. |
| PASS | strings.js > pupil > statScrollGraph | Scroll the graph sideways to see all of it. | Scrolling works the same on mouse, trackpad or finger. |
| PASS | strings.js > pupil > statNudgeLabel | Move it one square | "it" is whatever marker she's holding, right there on screen. |
| PASS | strings.js > pupil > statNudgeLeft | One square left | Clear. |
| PASS | strings.js > pupil > statNudgeRight | One square right | Clear. |
| PASS | strings.js > pupil > statNudgeUp | One square up | Clear. |
| PASS | strings.js > pupil > statNudgeDown | One square down | Clear. |
| PASS | strings.js > pupil > statRemovePoint | Take this point off | Plain verb-first. |
| PASS | strings.js > pupil > statReadWhy | Find every value you were asked for first. | Clear. |
| PASS | strings.js > pupil > statReadMoveWhy | Move the rule to the height you need first. | Clear, matches "the rule" already established. |
| PASS | strings.js > pupil > statReadAtXWhy | Move the rule across and put your answer in first. | Clear. |
| PASS | strings.js > pupil > statValueReadout | 65 | Real readout of her own working. |
| PASS | strings.js > pupil > statCfReadout | 65 | Same. |
| PASS | strings.js > pupil > statHowManyAbove | How many above | Plain label. |
| PASS | strings.js > pupil > statHowManyBelow | How many below | Plain label. |
| PASS | strings.js > pupil > statPctAbove | Percentage above | Plain label. |
| PASS | strings.js > pupil > statPctBelow | Percentage below | Plain label. |
| PASS | strings.js > pupil > statBoxPlaceWhy | Put all five markers on the scale first. | Clear, matches "markers" already established. |
| PASS | strings.js > pupil > statDrawBoxPlot | ✓ Draw the box plot | Names the diagram properly in full — contrast the pill below. |
| PASS | strings.js > pupil > statBoxDrawWhy | Draw the box plot first. | Clear. |
| PASS | strings.js > pupil > statBoxDrawnAlready | The box plot is drawn. | Plain status. |
| PASS | strings.js > pupil > statBoxStageWhy | Finish the first part before this one. | Fixed. No repeated "first" to stumble over; reads clean cold. |
| PASS | strings.js > pupil > statNextDrawBox | Next: draw the box plot | Clear, verb-first after the colon. |
| PASS | strings.js > pupil > statCompareWhy | Finish both sentences first. | Clear. |
| PASS | strings.js > pupil > statCompareValue | Value | Plain field label. |
| PASS | strings.js > pupil > statCmpHigherMedian | had the higher median | A sentence-builder chip, not a stray fragment — fine in that role. |
| PASS | strings.js > pupil > statCmpAgainst | against | Chip word. |
| PASS | strings.js > pupil > statCmpSoOnAverage | so on average | Chip word. |
| PASS | strings.js > pupil > statCmpWere | were | Chip word. |
| PASS | strings.js > pupil > statCmpHadThe | had the | Chip word. |
| PASS | strings.js > pupil > statCmpLarger | larger | Chip word. |
| PASS | strings.js > pupil > statCmpSmaller | smaller | Chip word. |
| PASS | strings.js > pupil > statCmpSoTheirs | so theirs were | Chip word. |
| PASS | strings.js > pupil > statCmpMore | more | Chip word. |
| PASS | strings.js > pupil > statCmpLess | less | Chip word. |
| PASS | strings.js > pupil > statCmpConsistent | consistent | Chip word. |
| PASS | strings.js > pupil > statFairToSay | Fair to say | Plain chip. |
| PASS | strings.js > pupil > statNotFair | Not fair | Short, complete, everyday phrase. |
| PASS | strings.js > pupil > statJudgeDecideWhy | Decide every claim first. | Clear. |
| PASS | strings.js > pupil > statJudgeReasonWhy | Give a reason for each one you called not fair. | Clear. |
| PASS | strings.js > pupil > statValuesStart | Choose a box and put your answer in. | Clear. |
| PASS | strings.js > pupil > statValuesWhy | Put a number in at least one box first. | Reads clean aloud despite the run of short words. |
| PASS | strings.js > pupil > statPillOrder | Put in order | Terse pill label, but sits beside its full instruction (`statStageQlistOrder`) — fine as a tracker name. |
| PASS | strings.js > pupil > statPillCuts | Find the quartiles | Fixed. Reuses the term the book has already established (the Ex.1 WALT), rather than the never-defined "cuts." |
| PASS | strings.js > pupil > statPillIqr | Find the interquartile range | Fixed. No more bare "IQR" — spells the term out in full, matching every other place it appears on the shelf. Four words rather than the two-or-three-word pill norm, but a full, unambiguous term beats a shorter banned abbreviation. |
| PASS | strings.js > pupil > statPillCftable | Fill the running total | Plain English, no jargon to decode. |
| PASS | strings.js > pupil > statPillValues | Work out the values | Plain, generic enough for its context. |
| PASS | strings.js > pupil > statPillPlot | Plot the points | Matches "points" already established elsewhere. |
| PASS | strings.js > pupil > statPillJoin | Join the points | Matches the `statJoinPoints` button exactly. |
| PASS | strings.js > pupil > statPillRead | Read the median | Clear, plain. |
| PASS | strings.js > pupil > statPillReadAt | Read at 30 | "30" is the real value for the question she's on, not a placeholder — appears beside the full stage instruction that names the axis it sits on. |
| PASS | strings.js > pupil > statPillBoxPlace | Place the markers | Matches "markers," already established via the tray fiction. |
| PASS | strings.js > pupil > statPillBoxDraw | Draw the box plot | Fixed, and better than the fix I suggested — spells out "box plot" in full, matching the commit button `statDrawBoxPlot` ("✓ Draw the box plot") exactly, and no longer collides with "Plot the points" used elsewhere for the cumulative-frequency graph. |
| PASS | strings.js > pupil > statPillCompare1 | The averages | A bare topic label for a stage tracker, in the same family as other plain nav headings ("My marks"). |
| PASS | strings.js > pupil > statPillCompare2 | The spread | Same reasoning. |
| PASS | strings.js > pupil > statPillJudge | Decide each one | Clear verb phrase. |
| PASS | strings.js > pupil > statStageQlistOrder | Build the ordered list. Choose the smallest value first, then the next smallest, and keep going until every value is in the row. (3 still to place.) | Says the whole act before any control: what to build, the exact method, and a live count of what's left. A model instruction. |
| PASS | strings.js > pupil > statStageQlistPick | Now choose the value you think is the median. If it sits between two values, choose both. Then press ✓ That's my median. | Action first, handles the tie-break case plainly, and names the exact button text before she has to hunt for it. |
| REWRITE | strings.js > pupil > statStageIqr | Enter the interquartile range on the pad. When it is in, press Mark my working. | The old fault (a bare "press ✓" naming no button) is gone, but the replacement names a button, "Mark my working," that belongs to a different book entirely — it's the algebra working-page's own control (see `pageFull`: "That is a full page — press Mark my working"), and doesn't match any of the seven task-specific stat check buttons this same shelf otherwise uses (`Mark my table`, `Mark my points and curve`, `Mark my readings`, `Mark my box plot`, `Mark my comparison`, `Mark my answers`). A pupil looking for a button called "Mark my working" on a statistics screen may not find one. This may be intentional if this stage genuinely shares the algebra page's bundle-then-mark flow, but that can't be confirmed from this transcript alone, and where unsure the rule is FAIL. → Name the real button on this screen — most likely, to match its sibling instruction below, "Enter the interquartile range on the pad, then press ✓ That's my answer." |
| PASS | strings.js > pupil > statStageIqrCommit | Enter the interquartile range on the pad, then press ✓ That's my answer. | New row (this key is new since the last read). Action-first, and — unlike its sibling `statStageIqr` above — correctly names the exact button she'll press, "✓ That's my answer," which is a real, already-established commit label (`statThatsMine`). This is the model the sibling instruction should follow. |
| PASS | strings.js > pupil > statStageCftable | Fill in the running total from the top row down: each box is the total so far plus that row's frequency. Choose a box, then use the pad. | Explains the mechanic precisely and correctly, action-first. |
| PASS | strings.js > pupil > statStageCfplotPlace | Put a point on the grid for every row of the table. Choose a spot to place a point; choose a placed point to move it with the arrows. (3 of 5 placed.) | Action-first, explains re-selection to move, live count. "The arrows" are the nudge-pad buttons visibly on screen, not a forward reference. |
| PASS | strings.js > pupil > statStageCfplotJoin | Every point is placed. Join them to draw the curve: press ✓ Join the points. | Names the exact button text, matching the strong pattern. |
| PASS | strings.js > pupil > statStageCfread | Move the rule up the frequency axis to the height for the median, then read where it meets the curve. Press ✓ That's my median to keep it. | Clear method, names the button. |
| PASS | strings.js > pupil > statStageCfreadAtX | Move the rule along the bottom axis to 30, and read the total where it meets the curve. Then enter the answer on the pad. | "30" is the real value for this question; clear method, right order. |
| PASS | strings.js > pupil > statStageBoxPlace | Choose a marker, then choose where it belongs on the scale. All five go on. (3 of 5 placed.) | Action-first, live count. |
| PASS | strings.js > pupil > statStageBoxDraw | All five markers are on the scale. Press ✓ Draw the box plot. | Names the exact button, and — unlike the pill above — spells out "box plot" in full. |
| PASS | strings.js > pupil > statStageCompare1 | Finish the first sentence: choose the group with the higher median, then enter both medians. | Clear method, right order. |
| PASS | strings.js > pupil > statStageCompare2 | Finish the second sentence: choose the measure, the group and the two values. | Clear list of what to choose. |
| PASS | strings.js > pupil > statStageJudge | For each statement choose Fair to say or Not fair. If you choose Not fair, choose the reason as well. | Names the exact chip labels she'll see, matching `statFairToSay`/`statNotFair` exactly. |
| PASS | strings.js > pupil > statStageJudgeOptions | Choose what happens to each one. | "Each one" plainly points at the visible list of sentence-completion chips in front of her. |
| PASS | strings.js > pupil > movieWorkedExample | Worked example | Plain heading. |
| PASS | strings.js > pupil > moviePrevStep | Previous step | Plain button. |
| PASS | strings.js > pupil > movieNextStep | Next step | Plain button. |
| PASS | strings.js > pupil > moviePlayLabel | Play | Plain. |
| PASS | strings.js > pupil > moviePlayBtn | ▶ Play | Icon plus word, self-explanatory. |
| PASS | strings.js > pupil > moviePauseBtn | ❚❚ Pause | Same. |
| PASS | strings.js > pupil > movieReplayBtn | ↺ Replay | Same. |
| PASS | strings.js > pupil > classifyYourAnswer | You answered "62" — your teacher can see this and will pick it up in class. | True to the screen, reassuring, no jargon. |
| PASS | strings.js > pupil > protractorCheckBtn | Mark my measurement | Fits what she did. |
| PASS | strings.js > pupil > protractorInstructions | Put the small crosshair at the centre of the protractor on the corner. Turn the protractor with a rotate knob at either end, until 0 sits along one arm, then read where the other arm crosses. | Thorough, concrete nouns, device-neutral verbs. |
| PASS | strings.js > pupil > protractorReadTrueWrongScale | You read the other scale — use the one that starts at 0 on the arm you lined up. The true size is 65°. | Clear, names the real slip. |
| PASS | strings.js > pupil > protractorReadTrueGeneric | Not quite — line the centre on the corner and 0 along an arm, then read again. The true size is 65°. | Clear instruction; true answer only shown once attempts are used up. |
| PASS | strings.js > pupil > protractorScaleRetry | Close — but check you are reading the scale that starts at 0 on your lined-up arm. One more go. | Clear. |
| PASS | strings.js > pupil > protractorLineUpRetry | Line it up carefully and read again — one more attempt. | Clear. |
| PASS | strings.js > pupil > protractorTypeFirst | Type the size you measured first. | Clear. |
| PASS | strings.js > pupil > protractorMaxRange | A protractor measures up to 180° — read the size again. | Clear, true fact. |
| PASS | strings.js > pupil > protractorMeasureLabel | Your measurement in degrees | Plain label. |
| PASS | strings.js > pupil > protractorMeasurePlaceholder | the size you measure, in degrees | A placeholder describing the expected format — fragments are the right shape for a placeholder. |
| PASS | strings.js > pupil > measuredReadout |  · you measured 65° | A trailing readout fragment appended to another string; fine in that role. |
| PASS | strings.js > pupil > checkLockedWhy | This one is finished. | Clear. |
| PASS | strings.js > pupil > checkNeedsLineWhy | Write a line of working first. | Clear. |
| PASS | strings.js > pupil > checkNeedsAngleWhy | Work out the angle you were asked for first. | Clear. |
| PASS | strings.js > pupil > addedToJotter | ✓ Added to your jotter. | Tick as icon, plain sentence. |
| PASS | strings.js > pupil > algebraNextLine | Write your next line of working: | Clear. |
| PASS | strings.js > pupil > removeLastLineBtn | ↶ remove last line | Icon plus words. |
| PASS | strings.js > pupil > removedLastLine | Removed your last line. | Plain status. |
| PASS | strings.js > pupil > lineTooLong | That line is too long for the page — split it into two steps. | Clear, tells her what to do. |
| PASS | strings.js > pupil > lineUnreadable | That line does not read as maths yet — check it and try again. (angles on a straight line add to 180) | Gives a concrete worked example alongside the correction. |
| PASS | strings.js > pupil > pageFull | That is a full page — press Mark my working. | Names the exact button. |
| PASS | strings.js > pupil > secondAttemptNote | Second attempt — your first try stays on the page. | Clear. |
| PASS | strings.js > pupil > algebraLinePlaceholder | your next line, then "add line" | Placeholder text, fine as a fragment. |
| PASS | strings.js > pupil > addLineBtn | add line | Plain button. |
| PASS | strings.js > pupil > moveAnnotationEquation | What are you doing to both sides? — tag the move (optional) | The checklist's must-fail list bans this exact phrase only when it appears on a question with no equation; this key is named for equation moves specifically, distinct from the generic version below, so it is being used in the context it was written for. |
| PASS | strings.js > pupil > moveAnnotationGeneric | What's your next step? — tag the move (optional) | Read together as rendered, a clear, optional, general question, nothing method-specific given away. |
| PASS | strings.js > pupil > chipCollectTerms | Collect terms | Plain chip. |
| PASS | strings.js > pupil > chipJustRewrite | Just rewrite | Plain chip. |
| PASS | strings.js > pupil > moveOperandLabel | How much? | Clear. |
| PASS | strings.js > pupil > moveOperandPlaceholder | how much? e.g. 15 or 3x | Read together as rendered, shows the input format only. |
| PASS | strings.js > pupil > moveOperandNext | next → | Clear with the arrow. |
| PASS | strings.js > pupil > substIntro | Choose each letter below to put its number in. | Clear. |
| PASS | strings.js > pupil > substGivenLabel | given: | Plain label. |
| PASS | strings.js > pupil > substAnswerPrompt | Now work it out, then enter the value: | Clear, right order. |
| PASS | strings.js > pupil > expandIntro | Multiply every term — choose the right product for each box. | Clear. |
| PASS | strings.js > pupil > solveChooseMove | Choose a move — you will see the next line, already checked for balance. | Clear, tells her what happens next. |
| PASS | strings.js > pupil > solveNeedNumber | Enter a number for that move first. | Clear. |
| PASS | strings.js > pupil > angleOneOpenHint | One angle is dashed on the diagram — choose it, give its size, and choose the reason. | Clear, points at something visible. |
| PASS | strings.js > pupil > angleMultiOpenHint | Choose a dashed angle on the diagram, give its size and reason, then work your way to angle a. | Fixed. "Angle a" now names the noun before the letter — no longer reads as an unfinished sentence. |
| PASS | strings.js > pupil > removeLastStepBtn | ↶ remove last step | Icon plus words. |
| PASS | strings.js > pupil > removedLastStep | Removed your last step. | Plain status. |
| PASS | strings.js > pupil > angleStepHeading | Work out ∠x | Standard maths glyph, self-explanatory alongside the labels around it. |
| PASS | strings.js > pupil > angleSizeLabel | Size of angle x | Plain label. |
| PASS | strings.js > pupil > angleSizePlaceholder | the size in degrees | Placeholder, fine as a fragment. |
| PASS | strings.js > pupil > angleUnreadable | That does not read as a number yet. Put in the size in degrees, or a sum that works it out. | Clear, gives two valid input forms. |
| PASS | strings.js > pupil > nudgeBannerStar | ★ Your teacher suggested watching this method. | Star as a visual flag; text otherwise identical to the approved `nudgeBanner`. |
| PASS | strings.js > pupil > selfEvalSavedFlash | ✓ Saved — your teacher sees this on her class list. | Tick as icon, plain sentence. |
| PASS | strings.js > pupil > selfEvalSavedIdle | Saved as you go — your teacher sees this on her class list. | Clear. |
| PASS | strings.js > pupil > angleUnreadableLine | That does not read as a number yet. Put in the size in degrees, or a sum that works it out. | Duplicate of `angleUnreadable`, same reasoning. |
| PASS | strings.js > teacher > artefactUndrawable | The pupil's work is saved, but this screen could not re-draw the board. | Fixed. "The pupil's" replaces the bare "Her" the §6 rule bans; no generic pronoun left. |
| PASS | strings.js > teacher > passcodeChecking | Checking the passcode… | Clear. |
| PASS | strings.js > teacher > tickSaving | Saving Angles for 10A-Maths… | Names the book and class plainly. |
| PASS | strings.js > teacher > overrideRight | Marked right — full marks. Your mark is the one pupils and staff now see. | New row (this key is new since the last read). Fixes both faults found against the equivalent live text in `MATHS_COLD_READ_VERDICTS_TEACHER.md`: "Marked" replaces the internal-jargon "Inked," and the plain "the one pupils and staff now see" replaces the undefined "wins on the Wall." Clean, reads plainly. |
| PASS | strings.js > teacher > overrideWrong | Marked wrong. Your mark is the one pupils and staff now see. | New row. Same two fixes as `overrideRight`, applied consistently. |
| PASS | strings.js > teacher > tickOn | Angles is now on 10A-Maths's shelf. | Clear confirmation. |
| PASS | strings.js > teacher > tickOff | Angles removed from 10A-Maths's shelf. | Clear. |
| PASS | strings.js > teacher > openMarkbook | Open the markbook | Plain button. |
| PASS | strings.js > teacher > noServer | We could not reach the server. Try again. | Clear. |
| PASS | strings.js > teacher > wallStale | This class page could not be refreshed. What you see is the last it loaded. | Plain, doesn't use the undefined "Wall" name flagged elsewhere. |
| PASS | strings.js > teacher > serverNotInitialised | This markbook has not been set up yet. Open Set-up and add a class first. | Clear, names the exact control. |
| PASS | strings.js > teacher > serverNotSignedIn | Google did not say who you are. Reload the page and sign in again. | Clear. |
| PASS | strings.js > teacher > serverBadName | That name could not be saved. Letters and spaces only. | Clear rule, stated plainly. |
| PASS | strings.js > teacher > serverUnknownClass | There is no class with that code. Check the link, or the code on the board. | Clear, gives two things to check. |
| PASS | strings.js > teacher > serverNotSet | That book is not switched on for this class. Tick it on in Set-up. | Clear reason, clear fix, names the exact screen. |
| PASS | strings.js > teacher > serverNotYourClass | That class was made by another teacher, so it is not yours to change. | Clear reason. |
| PASS | strings.js > teacher > serverExists | There is already a class with that name. | Clear. |
| PASS | strings.js > teacher > serverNotFound | That could not be found. Reload the page and try again. | Clear. |
| PASS | strings.js > teacher > serverUnreachableStore | The app cannot reach its own store, so nothing was saved and nothing was lost. Tell Damien the front door is not joined to the data deployment. | Names a real named contact for a real fault rather than inventing generic "support" — reassures her first (nothing lost) before asking her to act. |
| PASS | strings.js > teacher > serverTooBig | There is more working in this book than one save can carry. Tell Damien before the class writes any more. | Same pattern, clear. |
| PASS | strings.js > teacher > serverUnknownAction | The app asked for something this server does not know about. Reload the page. | Clear enough for a non-specialist. |
| PASS | strings.js > teacher > signedInAs | Signed in as aoife.gartland@c2ken.net | Plain confirmation. |
| PASS | strings.js > teacher > loadingClass | Loading 10A-Maths… | Clear. |
| PASS | strings.js > teacher > readingBooks | Reading 5 of 8 books… | Clear, live count. |
| PASS | strings.js > teacher > relockedLeft | The markbook closed when you left it. Enter the passcode to open it again. | Clear reason, clear fix. |
| PASS | strings.js > teacher > relockedIdle | The markbook closed itself after fifteen minutes. Enter the passcode to open it again. | Same, real number. |
| PASS | strings.js > teacher > inkChange | Tap or click a mark to change it. | Names both gestures. |
| PASS | strings.js > teacher > inkMine | Mark it right | Clear on its own; see the cross-check note below on how this compares with the button text actually rendered live. |
| PASS | strings.js > teacher > inkMineWrong | Mark it wrong | Clear on its own; same cross-check note applies. |
| PASS | strings.js > teacher > inkUseApp | Use the app's mark | Clear. |
| PASS | strings.js > teacher > needsYouLabel | Needs you now | Clear heading. |
| PASS | strings.js > teacher > needsYouWrongTwice | Wrong twice on Angles, Ex 2, Q4 | Clear, plain "Ex 2" not a glued code. |
| PASS | strings.js > teacher > needsYouPulledHelp | Used the method help on Angles, Ex 2, Q4 and is still wrong | Clear. |
| PASS | strings.js > teacher > needsYouStuck | Nothing saved for 20 minutes on Angles, Ex 2, Q4 | Clear, real number. |
| PASS | strings.js > teacher > csvCopied | Copied. Paste it into a spreadsheet. | Clear. |
| PASS | strings.js > teacher > csvFallback | Your browser would not let the page copy for you. Select the text below and copy it yourself. | Clear, blames the browser honestly rather than her. |
| PASS | strings.js > teacher > setUpHint | Tick the books this class should see. | Action-first. |
| PASS | strings.js > teacher > setUpClosedHint | A book that is not ticked is closed for that class: it is not on the pupils' shelf at all. | Clean, one consistent subject after the colon — matches the fixed line already confirmed in `_teacher.md`. |
| PASS | strings.js > teacher > overConfident | Over-confident — confidence high, working weaker | A legitimate teacher-facing analytic label, not pedagogy preached at a pupil. |
| PASS | strings.js > teacher > quietlyExcelling | Quietly excelling — doing well, low confidence | Same reasoning. |
| PASS | strings.js > teacher > cellWrongAtStep | Wrong at step 2 | Plain, matches the approved "step 3" form, not "line 3". |
| PASS | strings.js > teacher > cellWrongFirstSlip | Wrong — first slip at step 2 | Clear. |
| PASS | strings.js > teacher > markedAria | Marked right | Plain accessibility label. |
| PASS | strings.js > teacher > reteachTitle | Sends this exercise's worked example (Angles) to this pupil | "This pupil" is deixis pointing at a visible row, not a bare pronoun for someone unnamed. |
| PASS | strings.js > teacher > closeMarkbook | Close the markbook | Plain button. |
| PASS | strings.js > teacher > whereYouAre | Where you are | Plain heading. |
| PASS | strings.js > teacher > whichBook | Which book | Plain heading. |
| PASS | strings.js > teacher > needsYouAria | Pupils who need you now | Clear. |
| PASS | strings.js > teacher > nobodyStuck | Nobody is stuck in Angles right now. | Clear, reassuring. |
| PASS | strings.js > teacher > copyByHand | Copy this by hand: ols.link/10a | Plain, real link format. |
| PASS | strings.js > teacher > couldNotSave | We could not save that. | Clear. |
| PASS | strings.js > teacher > deleteClassAria | Delete 10A-Maths | Plain, names the real class. |
| PASS | strings.js > teacher > loadingGrid | Loading the full grid… | Clear. |
| PASS | strings.js > teacher > gridOrient | Each cell is one pupil and one question. Look for the reds, then open a cell to read that pupil's book. | Clear orientation; "the reds" is a conventional, low-risk shorthand for a school markbook grid. |
| PASS | strings.js > teacher > cellNotStarted | Not started | Plain. |
| PASS | strings.js > teacher > cellRight | Right — working and answer both sound | Clear. |
| PASS | strings.js > teacher > cellAmber | Answer only, no working shown | The checklist's own approved fixed form, word for word (§3). |
| PASS | strings.js > teacher > fetchingBook | Fetching this pupil's book… | Clear. |
| PASS | strings.js > teacher > pencilPosture | These marks are the app's. Tap or click any mark to change it to yours — yours is the one that counts. | The internal "pencil" naming only leaks into the key name, never into the rendered text itself, which is plain throughout. |
| PASS | strings.js > teacher > reteachBtn | Show them this method again → | Clear. |
| PASS | strings.js > teacher > reteachSent | Sent ✓ — this pupil sees it next time | Tick as icon, clear sentence. |
| PASS | strings.js > teacher > reteachFailed | We could not send that. | Clear. |
| PASS | strings.js > teacher > saveFailedMark | We could not save that — choose the mark again to try once more. | Clear, tells her what to do. |
| PASS | strings.js > teacher > readingEveryBook | Reading every book… | Clear. |
| PASS | strings.js > teacher > starterBtn | Next-lesson starter ▶ | Clear. |
| PASS | strings.js > teacher > csvCopiedFull | CSV copied — paste it straight into a spreadsheet. | "Paste it straight into a spreadsheet" tells her what to do even if "CSV" itself isn't glossed. |
| FAIL | strings.js > teacher > seriesKs3 | KS3 · M2 | Bare code "M2" nothing on screen defines — same fault as the book tags flagged in `_teacher.md` line 14. → "KS3 (Module 2)" |
| FAIL | strings.js > teacher > seriesGcse | GCSE · M3 & M4 | Same fault. → "GCSE (Modules 3 & 4)" |

**Counts for the string table above (267 rows): 264 PASS, 1 REWRITE, 2 FAIL.**

## The read-first candidate prompts carried in this transcript, judged by path (DFM 270 / §7)

The last 21 lines of `_v4.md` are the language gate's own read-first candidates, appended to this shell transcript so they get a read even though most belong to a book (`stats-quartiles`) or a book (Angles/Algebra) this file doesn't otherwise carry in full. Of the 66 candidates the gate named across the whole platform, these are the ones whose path and text appear literally, verbatim, in this file.

| verdict | where it is | the sentence | why |
|---|---|---|---|
| PASS | stats-quartiles > s1 > movie > step3 > say | Position of the median = (n + 1) ÷ 2.  /  Ring the 4th value. | Film content teaching the method with real, consistent working. |
| PASS | stats-quartiles > s1 > movie > step4 > say | Position of the lower quartile = (n + 1) ÷ 4 = 8 ÷ 4 = 2.  /  Ring the 2nd value. | Same reasoning, internally consistent. |
| PASS | stats-quartiles > s1 > movie > step5 > say | Position of the upper quartile = (n + 1) ÷ 4 × 3 = 2 × 3 = 6.  /  Ring the 6th value. | Same reasoning. |
| PASS | stats-quartiles > s1 > q2 > prompt | The speeds of 11 cars passing a speed camera, in mph: 19, 22, 26, 28, 28, 29, 29, 30, 30, 31, 36. | Real, picturable, no method leaked. |
| PASS | stats-quartiles > s1 > q6 > prompt | The ages of 11 trees, from youngest to oldest, are shown below. | Real, picturable; a pre-ordered list is genuine exam framing here, not a giveaway. |
| PASS | stats-quartiles > s1 > q7 > prompt | The scores, from lowest to highest, are shown below. | This fragment's antecedent ("a joke," "seven judges") sits one clause earlier in the book's full stem — anchored in context, not floating. |
| PASS | stats-quartiles > s1 > q8 > prompt | Say what happens to each average and to the range when it is added. | "It" is anchored one clause earlier in the full stem ("a wage of £110 was left off"). |
| PASS | stats-quartiles > s2 > q10 > prompt | The marks in an examination, out of 80, are grouped below for 112 candidates. | Real, picturable, clean sentence. |
| PASS | stats-quartiles > s3 > q13 > prompt | The marks in an examination, out of 80, are shown in the table below for 112 candidates. | Same reasoning. |
| PASS | stats-quartiles > s3 > q14 > prompt | In a survey, 184 people stated their weekly wage, grouped below. | Real, picturable. |
| PASS | stats-quartiles > s4 > q15 > prompt | The cumulative frequency graph shows the marks of 112 candidates in an examination.  /  What percentage of candidates passed? | The full stem states the pass mark (36) in the sentence between these two fragments, so "passed" has one defensible answer in context. |
| REWRITE | stats-quartiles > s5 > q19 > prompt | The table shows some information about the heights, in cm, of some plants. | Sets up the exercise's very first box plot with no plain-words line anywhere before it — matches the finding against this same stem in `MATHS_COLD_READ_VERDICTS_stats-quartiles.md`. → "The table shows some information about the heights, in cm, of some plants. Draw a box plot — a diagram showing the lowest, highest and middle values — for this information. [2 marks]" |
| PASS | stats-quartiles > s5 > q20 > prompt | The table shows some information about the weights, in grams, of some potatoes: range 101 g, lower quartile 110 g, median 132 g, upper quartile 162 g, maximum 185 g. | Anchored; the full stem's "work it out, then draw" is the right action-order. |
| PASS | stats-quartiles > s5 > q21 > prompt | The times, in seconds, of 15 students running a race are shown below: 52, 54, 54, 55, 58, 58, 59, 60, 60, 61, 61, 64, 67, 70, 75. | Real, picturable, a raw list she must reduce herself. |
| PASS | stats-quartiles > s5 > q24 > prompt | The box plots show the age distributions of two cities, A and B. Decide which city has the greater interquartile range, and which city's people are generally older. | Properly anchored — tells her plainly two diagrams are in front of her. |
| PASS | stats-quartiles > s5 > q27 > prompt | The table shows information about the times, in minutes, that some boys took to complete a puzzle: interquartile range 8, minimum 12, median 18, upper quartile 23, maximum 29. | Real, picturable, no padding. |
| PASS | stats-quartiles > s6 > q29 > prompt | Decide whether his survey gives a fair picture of the town, and why. | Closes an anchored, named stem earlier in the same sentence group. |
| PASS | stats-quartiles > s6 > q30 > prompt | Decide whether this survey gives a fair picture of everyone, and why. | Closes a concrete, anchored stem. |
| PASS | stats-quartiles > s6 > q31 > prompt | Decide whether each survey gives a fair picture of the town, and why. | Closes a stem with clean pronoun reference throughout. |
| PASS | stats-quartiles > s6 > q32 > prompt | Decide whether each survey gives a fair picture, and why. | Closes a stem with three named, self-contained scenarios; "sample" is well-practised under "survey" by this point. |
| PASS | strings.js > pupil > classifyTryAgain | Not that one — look again at its size against 90° and 180°. | A fair retry hint reminding her of the classification boundary, not the answer itself. |

**Counts for the read-first section (21 rows): 20 PASS, 1 REWRITE.**

**Total for this file (288 rows): 284 PASS, 2 REWRITE, 2 FAIL.**

## A cross-check worth raising outside the table

Two things this file's own content contradicts against the live teacher screen, both worth a human look before this ships:
1. This file now carries `overrideRight` / `overrideWrong` with the fixed wording ("Marked right — full marks. Your mark is the one pupils and staff now see." / "Marked wrong. Your mark is the one pupils and staff now see."), and both PASS above. But `_teacher.md`, re-read at the same time as this update, still renders the OLD text at its lines 46–47 — "Inked right — full marks. Your mark wins on the Wall." / "Inked wrong. Your mark wins on the Wall." — word for word unchanged, under the SAME transcript hash as before (`93e87394fa08bf67`). The fix has landed in this shell's string table but not yet on the live teacher screen; `MATHS_COLD_READ_VERDICTS_TEACHER.md` still correctly FAILs those two lines as actually rendered, and its hash has not been touched, because nothing there has actually changed yet.
2. This file's `inkMine` / `inkMineWrong` / `inkUseApp` keys read "Mark it right" / "Mark it wrong" / "Use the app's mark", while `_teacher.md` actually renders "✓ mine" / "✗ mine" / "use the app's mark" for what looks like the same control. This may simply be a fuller accessibility label sitting behind a shorter glyph button — a legitimate and common pattern — but it's worth confirming these are the same control and not two copies of its text drifting apart.
