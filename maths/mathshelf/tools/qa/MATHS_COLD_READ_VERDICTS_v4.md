TRANSCRIPT HASH: 047ed679302b7058

Artefact judged: MathShelf — the v4 transcript, 146 sentences, taken 2026-09-06 08:19:52.
Read cold, aloud, one sentence at a time, from the reader's seat. Counts: 88 pass, 3 fail, 55 rewrite.

| Verdict | Where it is | The sentence | Why |
|---|---|---|---|
| PASS | strings.js > pupil > coverBusy | Opening your books… | Four plain words, and "your books" is the thing she came here for. |
| REWRITE | strings.js > pupil > coverGetting | Getting your details… | "Your details" is not something she can picture, and she is not told what is being fetched or why it concerns her; replacement: "Getting your name…" |
| PASS | strings.js > pupil > coverWelcome | Welcome | One warm word with nothing to decode. |
| PASS | strings.js > pupil > coverOpen | Open your books | The action leads, the noun is concrete, and no gesture is named, so it is true for a finger and for a mouse. |
| REWRITE | strings.js > pupil > coverSwitch | Not you? Switch account | A question at the front door of the same kind that has failed before, and "Switch account" never says what will happen to her; replacement: "Sign in as someone else." |
| REWRITE | strings.js > pupil > coverFirstTime | First time here? Google will ask your permission once, then you are straight in. | It opens with a question, hands the sentence to Google instead of to her, and never says what the permission is for, so she cannot say yes knowing what she agreed to; replacement: "The first time you open your books, Google asks you once to let MathShelf read your name. Say yes, and your books open." |
| REWRITE | strings.js > pupil > coverNoServer | We could not reach the server. Check your connection and reload the page. | "The server" is a machine she has never seen on any screen, and telling a girl revising at home to check her connection blames her for something that may not be hers; replacement: "Your books did not load just now. Open the page again in a moment." |
| REWRITE | strings.js > pupil > coverWrongClass | That class link is not active. Ask your teacher for the link again. | "Not active" is a word she would have to guess at, and at home there is nobody in the room to ask; replacement: "This link does not open a class. Ask your teacher for the right link." |
| PASS | strings.js > pupil > coverNameLabel | Your name | It names the box and does nothing else. |
| REWRITE | strings.js > pupil > coverNamePrompt | We could not read your name from your account. Write it once and it is saved. | The first sentence is clean, but "Write it once and it is saved" trips on the change of tense and never says where to write it; replacement: "We could not read your name from your account. Write it in the box below, and MathShelf will remember it." |
| PASS | strings.js > pupil > coverNameMissing | Write your name first, so your teacher sees it on her class list. | The action comes first and the reason follows, and a class list is a thing she has watched a teacher hold. |
| REWRITE | strings.js > pupil > coverPreview | This is the preview copy of MathShelf. Nothing here is saved to school. | "Preview copy" and "saved to school" both have to be worked out, and the one thing she needs — that this work does not count — is left for her to infer; replacement: "This is a practice copy of MathShelf. Nothing you do here is sent to your teacher." |
| PASS | strings.js > pupil > shelfGreetingMorning | Good morning | Two words, no work to do. |
| PASS | strings.js > pupil > shelfGreetingAfternoon | Good afternoon | Two words, no work to do. |
| PASS | strings.js > pupil > shelfPlain | Your maths books live here. Your teacher chooses which ones are out. | Two short sentences that say whose the books are and who decides. |
| PASS | strings.js > pupil > shelfNotSet | Not set yet | "Set" would be a risky word on a maths shelf, but the note beside it says plainly what it means here. |
| REWRITE | strings.js > pupil > shelfNotSetNote | A book that is not set yet is one your teacher has not put out for this class. | A definition folded inside two relative clauses; I read it twice before I found the person in it; replacement: "Your teacher has not put this book out for your class yet." |
| PASS | strings.js > pupil > shelfMore | Your teacher will add more books to this shelf during the year. | Person first, concrete, and it answers the obvious question about the empty spaces. |
| FAIL | strings.js > pupil > shelfMarks | {got} of {max} marks · {done} of 8 answered | As rendered her shelf shows raw braces where the numbers should be, and the 8 is baked into the wording instead of being the real number of questions. |
| PASS | strings.js > pupil > backToShelf | The shelf | It names where the link goes, in the word the shelf itself used. |
| PASS | strings.js > pupil > opening | Opening Angles… | It names the book that is opening. |
| PASS | strings.js > pupil > nudgeBanner | Your teacher suggested watching this method. | It says who sent it and what it is, without nagging or explaining itself. |
| PASS | strings.js > pupil > selfEvalSaved | Saved. Your teacher sees this on her class list. | Two short sentences, and it is honest about who will see it. |
| PASS | strings.js > pupil > selfEvalHeading | Exercise finished — how did that go? | The question is one the screen means her to answer, not one she is left holding. |
| PASS | strings.js > pupil > selfEvalOptional | optional | One ordinary school word telling her she may skip it. |
| PASS | strings.js > pupil > saveWaiting | Still saving your work… | Plain, and it settles the only worry she has at that moment. |
| PASS | strings.js > pupil > saveRetry | Try again | It names the action. |
| REWRITE | strings.js > pupil > saveHeldLocal | Your work is safe on this device and will be sent as soon as the page can reach the server. | One long sentence that ends on two machine nouns she cannot picture, at the moment she most needs reassuring; replacement: "Your work is safe on this device. Nothing is lost — MathShelf will send it as soon as it can." |
| PASS | strings.js > pupil > movieWorkedExample | Worked example | Her maths teacher's own words for the thing on the screen. |
| PASS | strings.js > pupil > moviePrevStep | Previous step | It says where the button goes. |
| PASS | strings.js > pupil > movieNextStep | Next step | It says where the button goes. |
| PASS | strings.js > pupil > moviePlayLabel | Play | One word she has pressed a thousand times. |
| PASS | strings.js > pupil > moviePlayBtn | ▶ Play | The word carries the meaning and the arrow only decorates it. |
| PASS | strings.js > pupil > moviePauseBtn | ❚❚ Pause | The word carries the meaning and the glyph only decorates it. |
| PASS | strings.js > pupil > movieReplayBtn | ↺ Replay | The word carries the meaning and the glyph only decorates it. |
| REWRITE | strings.js > pupil > classifyYourAnswer | You answered “{answer}” — your teacher can see this and will pick it up in class. | The raw brace prints on her screen where her answer should be, and "pick it up in class" is an idiom that can read like trouble coming; replacement: "You answered “obtuse”. Your teacher can see your answer and will talk about it in class." |
| PASS | strings.js > pupil > protractorCheckBtn | Mark my measurement | It matches the other Check buttons and says exactly what pressing it does. |
| REWRITE | strings.js > pupil > protractorInstructions | Put the small crosshair at the centre of the protractor on the corner. Turn the protractor with a rotate knob at either end, until 0 sits along one arm, then read where the other arm crosses. | Three separate steps run together as prose when they should be a numbered list, and the first stacks "at the centre of the protractor on the corner" so I had to read it twice to see what goes where; replacement, as a list: "1. Put the middle of the protractor on the corner of the angle. 2. Turn the protractor by the round knob at each end, until 0 sits along one arm. 3. Read the number where the other arm crosses." |
| PASS | strings.js > pupil > protractorReadTrueWrongScale | You read the other scale — use the one that starts at 0 on the arm you lined up. The true size is 65°. | It names the slip she actually made, gives her the rule for next time, and only hands over the true size once her attempts are spent. |
| REWRITE | strings.js > pupil > protractorReadTrueGeneric | Not quite — line the centre on the corner and 0 along an arm, then read again. The true size is 65°. | "line the centre on the corner and 0 along an arm" drops the "up" out of "line up" and then hangs a second object off the same verb, so it reads wrong aloud; replacement: "Not quite — put the middle of the protractor on the corner, turn it until 0 sits along one arm, then read again. The true size is 65°." |
| REWRITE | strings.js > pupil > protractorScaleRetry | Close — but check you are reading the scale that starts at 0 on your lined-up arm. One more go. | Three ideas are hung off one another in a single clause, and "your lined-up arm" is not English she would ever say; replacement: "Close — read the scale that starts at 0 on the arm you lined up. One more go." |
| REWRITE | strings.js > pupil > protractorLineUpRetry | Line it up carefully and read again — one more attempt. | "Line it up" leaves "it" unnamed when there are three things on the screen it could mean; replacement: "Line the protractor up again carefully, then read the size. One more go." |
| REWRITE | strings.js > pupil > protractorTypeFirst | Type the size you measured first. | "the size you measured first" reads as the first size she measured; replacement: "First put in the size you measured." |
| PASS | strings.js > pupil > protractorMaxRange | A protractor measures up to 180° — read the size again. | One fact, one instruction, and it does not say what the answer is. |
| PASS | strings.js > pupil > protractorMeasureLabel | Your measurement in degrees | It names the box and the units. |
| PASS | strings.js > pupil > protractorMeasurePlaceholder | the size you measure, in degrees | A ghost line that gives her the units and no hint of a value. |
| PASS | strings.js > pupil > measuredReadout |  · you measured 65° | It hangs off the mark line and repeats back what she did, in her own terms. |
| REWRITE | strings.js > pupil > checkLockedWhy | This one is finished. | "This one" is a bare demonstrative doing the work a noun should do; replacement: "You have finished this question." |
| PASS | strings.js > pupil > checkNeedsLineWhy | Write a line of working first. | Short, the action leads, and a line of working is the thing the page has already asked her for. |
| REWRITE | strings.js > pupil > checkNeedsAngleWhy | Work out the angle you were asked for first. | The "first" lands at the end and turns the sentence into "the angle you were first asked for"; replacement: "First work out the angle you were asked for." |
| REWRITE | strings.js > pupil > addedToJotter | ✓ Added to your jotter. | Nothing she has seen has been called a jotter — the thing she writes on is the page; replacement: "✓ Added to your page." |
| PASS | strings.js > pupil > algebraNextLine | Write your next line of working: | The action leads, the noun is concrete, and it sits above the box. |
| PASS | strings.js > pupil > removeLastLineBtn | ↶ remove last line | It says exactly what it undoes. |
| PASS | strings.js > pupil > removedLastLine | Removed your last line. | Four words confirming the undo. |
| PASS | strings.js > pupil > lineTooLong | That line is too long for the page — split it into two steps. | It says what is wrong and what to do instead, without touching the maths. |
| REWRITE | strings.js > pupil > lineUnreadable | That line does not read as maths yet — check it and try again. ({reason}) | The raw brace prints on her screen, and a reason bracketed on the end is exactly the sort of code she has to decode; replacement, with the bracket deleted: "That line does not read as maths yet. Check it and try again." |
| REWRITE | strings.js > pupil > pageFull | That is a full page — press Mark my working. | The button is named exactly as it is printed, but "That is" is the bare demonstrative opener that has failed his reads before; replacement: "Your page is full — press Mark my working." |
| PASS | strings.js > pupil > secondAttemptNote | Second attempt — your first try stays on the page. | It answers the one thing she would worry about: her first try is not being wiped. |
| REWRITE | strings.js > pupil > algebraLinePlaceholder | your next line, then “add line” | The instruction is hidden inside the box, so it disappears the moment she starts typing; replacement, with "then press add line" moved to a line above the box: "your next line of working" |
| PASS | strings.js > pupil > addLineBtn | add line | Two words, and the box beside it points straight at them. |
| REWRITE | strings.js > pupil > moveAnnotationEquation | What are you doing to both sides? — tag the move (optional) | The question is right for an equation, but "tag the move" is platform language she has never been given; replacement: "What are you doing to both sides? Choose what you did — this is optional." |
| REWRITE | strings.js > pupil > moveAnnotationGeneric | What’s your next step? — tag the move (optional) | Same platform language: she is asked to "tag" something, in a word no screen has taught her; replacement: "What is your next step? Choose what you did — this is optional." |
| PASS | strings.js > pupil > chipCollectTerms | Collect terms | Her maths teacher's own phrase, and it answers the question above it. |
| REWRITE | strings.js > pupil > chipJustRewrite | Just rewrite | Even under a question it still reads like an order to rewrite rather than a description of what she did; replacement: "Rewrite with no change" |
| REWRITE | strings.js > pupil > moveOperandLabel | How much? | Two words that never say how much of what; replacement: "What number did you use?" |
| REWRITE | strings.js > pupil > moveOperandPlaceholder | how much? e.g. 15 or 3x | "e.g." is an abbreviation she has to decode, inside the very box she is about to type in; replacement: "for example 15 or 3x" |
| PASS | strings.js > pupil > moveOperandNext | next → | It says where it takes her. |
| REWRITE | strings.js > pupil > substIntro | Choose each letter below to put its number in. | "to put its number in" leaves the "in" hanging with nowhere named; replacement: "Choose a letter below, then put in the number it stands for. Do this for every letter." |
| PASS | strings.js > pupil > substGivenLabel | given: | The exam's own word for the numbers she has been handed. |
| PASS | strings.js > pupil > substAnswerPrompt | Now work it out, then enter the value: | Two steps in the order she does them, and "it" points back at the expression she has just filled. |
| REWRITE | strings.js > pupil > expandIntro | Multiply every term — choose the right product for each box. | "Multiply every term" is the method the question is testing, handed to her in the stem; replacement: "Fill every box — choose the product that belongs in it." |
| REWRITE | strings.js > pupil > solveChooseMove | Choose a move — you will see the next line, already checked for balance. | "already checked for balance" is machinery rather than maths she can picture, and it arrives before she knows what a move is; replacement: "Choose what to do to both sides. The next line is then written out for you." |
| REWRITE | strings.js > pupil > solveNeedNumber | Enter a number for that move first. | "that move" points at nothing named, and the trailing "first" reads as "the first move"; replacement: "First put in the number you want to use." |
| REWRITE | strings.js > pupil > angleOneOpenHint | One angle is dashed on the diagram — choose it, give its size, and choose the reason. | Three separate actions are strung into one sentence when they should be a numbered list; replacement: "One angle on the diagram is drawn with a dashed line. 1. Choose that angle. 2. Put in its size in degrees. 3. Choose the reason." |
| REWRITE | strings.js > pupil > angleMultiOpenHint | Choose a dashed angle on the diagram, give its size and reason, then work your way to {target}. | The raw brace prints, three actions run together in one sentence, and "work your way to" is an idiom; replacement: "Some angles on the diagram are drawn with dashed lines. 1. Choose a dashed angle. 2. Put in its size in degrees. 3. Choose the reason. Keep going until you have the angle you were asked for." |
| PASS | strings.js > pupil > removeLastStepBtn | ↶ remove last step | It says exactly what it undoes. |
| PASS | strings.js > pupil > removedLastStep | Removed your last step. | Four words confirming the undo. |
| REWRITE | strings.js > pupil > angleStepHeading | Work out ∠Aoife | The angle is named after a person, and the ∠ sign is a glyph she has to decode when the label directly beneath spells "angle" out in words; replacement: "Work out angle x", with the diagram's own label in place of x and never a pupil's name. |
| REWRITE | strings.js > pupil > angleSizeLabel | Size of angle Aoife | The same person's name is standing where the angle's label belongs, so the box asks for the size of a girl; replacement: "Size of angle x", with the diagram's own label in place of x. |
| PASS | strings.js > pupil > angleSizePlaceholder | the size in degrees | It gives her the units and no hint of a value. |
| REWRITE | strings.js > pupil > angleUnreadable | That does not read as a number yet. Put in the size in degrees, or a sum that works it out. | "a sum that works it out" makes "it" do double duty and sent me back to the start of the sentence; replacement: "That does not read as a number yet. Put in the size in degrees. You can write a sum instead, and MathShelf will work it out." |
| PASS | strings.js > pupil > nudgeBannerStar | ★ Your teacher suggested watching this method. | The approved nudge line with a marker in front of it; the sentence still says who sent it and what it is. |
| PASS | strings.js > pupil > selfEvalSavedFlash | ✓ Saved — your teacher sees this on her class list. | Short, and honest about who sees it. |
| PASS | strings.js > pupil > selfEvalSavedIdle | Saved as you go — your teacher sees this on her class list. | "As you go" is everyday English and it answers the worry about losing work. |
| REWRITE | strings.js > pupil > angleUnreadableLine | That does not read as a number yet. Put in the size in degrees, or a sum that works it out. | Word for word the same message as the angleUnreadable line, and it trips in the same place; replacement: "That does not read as a number yet. Put in the size in degrees. You can write a sum instead, and MathShelf will work it out." |
| PASS | strings.js > teacher > passcodeChecking | Checking the passcode… | Teacher screen: three words, and she knows to wait. |
| PASS | strings.js > teacher > openMarkbook | Open the markbook | Teacher screen: the button says what it opens. |
| PASS | strings.js > teacher > noServer | We could not reach the server. Try again. | Teacher screen: a colleague at a laptop reads "the server" without stopping, and the next move is named. |
| PASS | strings.js > teacher > serverNotInitialised | This markbook has not been set up yet. Open Set-up and add a class first. | Teacher screen: it says what is missing and names Set-up as the place to fix it. |
| PASS | strings.js > teacher > serverNotSignedIn | Google did not say who you are. Reload the page and sign in again. | Teacher screen: blunt, but between lessons she knows instantly what to do. |
| PASS | strings.js > teacher > serverBadName | That name could not be saved. Letters and spaces only. | Teacher screen: it gives the rule that was broken, so she can fix the name without asking anyone. |
| REWRITE | strings.js > teacher > serverUnknownClass | There is no class with that code. Check the link, or the code on the board. | Teacher screen: "the code on the board" could be the whiteboard or something in the app, and she has no way to tell which; replacement: "There is no class with that code. Check the class link, or the class code you gave the pupils." |
| PASS | strings.js > teacher > serverNotSet | That book is not switched on for this class. Tick it on in Set-up. | Teacher screen: it names the tick and the place to do it. |
| PASS | strings.js > teacher > serverNotYourClass | That class was made by another teacher, so it is not yours to change. | Teacher screen: the control that will not act says why, in one line. |
| PASS | strings.js > teacher > serverExists | There is already a class with that name. | Teacher screen: one fact, and the fix follows from it without being spelled out. |
| REWRITE | strings.js > teacher > serverNotFound | That could not be found. Reload the page and try again. | Teacher screen: "That" names nothing, so she cannot tell whether a class, a book or a pupil has gone missing; replacement: "That class or book could not be found. Reload the page and try again." |
| REWRITE | strings.js > teacher > serverUnreachableStore | The app cannot reach its own store, so nothing was saved and nothing was lost. Tell Damien the front door is not joined to the data deployment. | Teacher screen: "its own store", "the front door" and "the data deployment" are the builder's words, and she cannot pass on a message she does not understand; replacement: "Nothing was saved just now, and nothing was lost. Tell Damien that the markbook cannot reach where the work is kept." |
| REWRITE | strings.js > teacher > serverTooBig | There is more working in this book than one save can carry. Tell Damien before the class writes any more. | Teacher screen: "more working than one save can carry" describes the machinery instead of what has happened to her class's work; replacement: "This book now has too much working in it to save. Tell Damien before the class writes any more." |
| REWRITE | strings.js > teacher > serverUnknownAction | The app asked for something this server does not know about. Reload the page. | Teacher screen: it reports a conversation between two pieces of software she cannot see, and leaves her nothing to say to anyone; replacement: "Something went wrong between the page and the markbook. Reload the page, and tell Damien if it happens again." |
| PASS | strings.js > teacher > signedInAs | Signed in as aoife.gartland@c2ken.net | Teacher screen: the wording is right, though the example address shown here is a pupil's when it should be the signed-in teacher's. |
| PASS | strings.js > teacher > loadingClass | Loading 10A-Maths… | Teacher screen: it names the class being loaded. |
| FAIL | strings.js > teacher > readingBooks | Reading {done} of 8 books… | Teacher screen: the raw brace prints instead of a number, and the 8 is fixed in the wording rather than being the real number of books. |
| PASS | strings.js > teacher > relockedLeft | The markbook closed when you left it. Enter the passcode to open it again. | Teacher screen: it says why the markbook shut and what to do about it. |
| PASS | strings.js > teacher > relockedIdle | The markbook closed itself after fifteen minutes. Enter the passcode to open it again. | Teacher screen: the fifteen minutes is real and it matters, and the fix comes in the same breath. |
| PASS | strings.js > teacher > inkChange | Tap or click a mark to change it. | Teacher screen: it names both ways in, for a smartboard and for a laptop. |
| PASS | strings.js > teacher > inkMine | Mark it right | Teacher screen: the button says what the press does. |
| PASS | strings.js > teacher > inkMineWrong | Mark it wrong | Teacher screen: the button says what the press does. |
| PASS | strings.js > teacher > inkUseApp | Use the app’s mark | Teacher screen: it names the third option plainly, and the line beside it explains whose mark counts. |
| PASS | strings.js > teacher > needsYouLabel | Needs you now | Teacher screen: the rows underneath say what each pupil needs, so the heading is answered where she reads it. |
| FAIL | strings.js > teacher > needsYouWrongTwice | Wrong twice on Angles, {exercise}, {question} | Teacher screen: the raw braces print, and a bare comma list will not tell her which number is the exercise and which the question. |
| REWRITE | strings.js > teacher > needsYouPulledHelp | Used the method help on Angles, {exercise}, {question} and is still wrong | Teacher screen: the raw braces print, and "the method help" is a third name for the thing the pupil's own screen calls a worked example; replacement: "Watched the worked example on Angles, Exercise 2, Question 3, and is still wrong" |
| REWRITE | strings.js > teacher > needsYouStuck | Nothing saved for {minutes} minutes on Angles, {exercise}, {question} | Teacher screen: the raw braces print, and "Nothing saved" reads like a save failure on a markbook that shows real save failures elsewhere, when it means the pupil has written nothing; replacement: "Has written nothing for 6 minutes on Angles, Exercise 2, Question 3" |
| PASS | strings.js > teacher > csvCopied | Copied. Paste it into a spreadsheet. | Teacher screen: it confirms the copy and names the next move, with no file jargon. |
| PASS | strings.js > teacher > csvFallback | Your browser would not let the page copy for you. Select the text below and copy it yourself. | Teacher screen: it says why the button could not do it and exactly what she must do instead. |
| PASS | strings.js > teacher > setUpHint | Tick the books this class should see. | Teacher screen: the action leads and the effect is stated in the same line. |
| REWRITE | strings.js > teacher > overConfident | Over-confident — confidence high, working weaker | Teacher screen: the tail is two verbless scraps joined on a comma, and the label brands the pupil rather than describing the work; replacement: "Rated this work as easy, but the working is weaker." |
| REWRITE | strings.js > teacher > quietlyExcelling | Quietly excelling — doing well, low confidence | Teacher screen: a flourish rather than a finding, with the same pair of verbless scraps behind the dash; replacement: "Is doing well, but rated this work as hard." |
| PASS | strings.js > teacher > cellWrongAtStep | Wrong at step 2 | Teacher screen: "step 2" is in the words she and the pupil both use, not a code. |
| REWRITE | strings.js > teacher > cellWrongFirstSlip | Wrong — first slip at step 2 | Teacher screen: she cannot tell how this differs from the cell reading "Wrong at step 2", and "slip" is platform language never explained to her; replacement: "Wrong — the first mistake is at step 2, and there are more after it." |
| PASS | strings.js > teacher > markedAria | Marked right | Teacher screen: a plain read-aloud label with one meaning. |
| REWRITE | strings.js > teacher > reteachTitle | Sends this exercise’s worked example (Angles) to this pupil | Teacher screen: the bracket interrupts the sentence and "this exercise's worked example" is a double possessive that sent me back to the start; replacement: "Sends this pupil the worked example from this exercise in Angles." |
| PASS | strings.js > teacher > closeMarkbook | Close the markbook | Teacher screen: the button says what it closes. |
| PASS | strings.js > teacher > whereYouAre | Where you are | Teacher screen: a heading over the trail of class, book and question, and it says what it is. |
| PASS | strings.js > teacher > whichBook | Which book | Teacher screen: it labels the book chooser and nothing else. |
| PASS | strings.js > teacher > needsYouAria | Pupils who need you now | Teacher screen: it says in full what the short heading means. |
| PASS | strings.js > teacher > nobodyStuck | Nobody is stuck in Angles right now. | Teacher screen: a real answer to a real glance, and it names the book. |
| PASS | strings.js > teacher > copyByHand | Copy this by hand: ols.link/10a | Teacher screen: it says what to do with the link when the copy button cannot do it for her. |
| REWRITE | strings.js > teacher > couldNotSave | We could not save that. | Teacher screen: "that" names nothing and no next step is offered, so she is left holding a question between lessons; replacement: "That did not save — try once more." |
| PASS | strings.js > teacher > deleteClassAria | Delete 10A-Maths | Teacher screen: it names exactly what will be deleted. |
| PASS | strings.js > teacher > loadingGrid | Loading the full grid… | Teacher screen: it names what is coming. |
| REWRITE | strings.js > teacher > gridOrient | Each cell is one pupil and one question. Look for the reds, then open a cell to read that pupil's book. | Teacher screen: "the reds" leans on colour alone with no key saying what red means, which fails a colour-blind colleague and a smartboard seen from the back of a room; replacement: "Each cell is one pupil and one question. A red cell is a wrong answer — open a cell to read that pupil's book." |
| PASS | strings.js > teacher > cellNotStarted | Not started | Teacher screen: two words with one meaning. |
| REWRITE | strings.js > teacher > cellRight | Right — working and answer both sound | Teacher screen: "sound" is doing quiet work as an adjective and can be misread as the noun on first glance; replacement: "Right — working and answer both correct" |
| REWRITE | strings.js > teacher > cellAmber | Answer only, no working shown | Teacher screen: the approved wording joins these two halves with a dash; on a comma it is two verbless scraps; replacement: "Answer only — no working shown" |
| PASS | strings.js > teacher > fetchingBook | Fetching this pupil's book… | Teacher screen: it names what is loading and whose it is. |
| PASS | strings.js > teacher > pencilPosture | These marks are the app's. Tap or click any mark to change it to yours — yours is the one that counts. | Teacher screen: whose the marks are, how to change them and whose count, in one read and both gestures named. |
| PASS | strings.js > teacher > reteachBtn | Show them this method again → | Teacher screen: the action leads, and "them" keeps a gender off a named pupil. |
| PASS | strings.js > teacher > reteachSent | Sent ✓ — this pupil sees it next time | Teacher screen: she has just pressed send, so the confirmation lands with no gap to fill. |
| REWRITE | strings.js > teacher > reteachFailed | We could not send that. | Teacher screen: "that" names nothing and there is no next step; replacement: "That did not send — try once more." |
| PASS | strings.js > teacher > saveFailedMark | We could not save that — choose the mark again to try once more. | Teacher screen: it says what failed and names the exact thing to press again. |
| PASS | strings.js > teacher > readingEveryBook | Reading every book… | Teacher screen: plain, and it explains the wait. |
| PASS | strings.js > teacher > starterBtn | Next-lesson starter ▶ | Teacher screen: "starter" is the department's own word for the opening task, so a maths colleague reads it at a glance. |
| REWRITE | strings.js > teacher > csvCopiedFull | CSV copied — paste it straight into a spreadsheet. | Teacher screen: it puts a file abbreviation on screen where the other copy message manages perfectly well without one; replacement: "Copied — paste it straight into a spreadsheet." |
| REWRITE | strings.js > teacher > seriesKs3 | KS3 · M2 | Teacher screen: M1 to M8 are CCEA's GCSE unit codes, so an M number sitting beside KS3 is a code a colleague would have to ask about; replacement: "KS3 books" |
| PASS | strings.js > teacher > seriesGcse | GCSE · M3 & M4 | Teacher screen: M3 and M4 are CCEA's own unit codes, which is exactly the language a maths colleague already uses. |
| REWRITE | strings.js > pupil > classifyTryAgain | Not that one — look again at its size against 90° and 180°. | "its size against" turns the sentence back on itself, and "its" points at an angle the sentence never names; replacement: "Not that one — compare the angle with 90° and with 180°, then choose again." |
