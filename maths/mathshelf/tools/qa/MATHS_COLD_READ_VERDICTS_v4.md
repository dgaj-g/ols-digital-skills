TRANSCRIPT HASH: 295cc3715530afe0

| Verdict | Where it is | The sentence | Why |
|---|---|---|---|
| PASS | strings.js > pupil > coverBusy | Opening your books… | Plain and concrete: she can picture her books opening, and there is nothing to do but wait. |
| REWRITE | strings.js > pupil > coverGetting | Getting your details… | “Your details” is a thing she cannot picture and she would have to ask which details — use: “Checking who you are…” |
| PASS | strings.js > pupil > coverWelcome | Welcome | One warm word at the front door, with nothing to decode. |
| PASS | strings.js > pupil > coverOpen | Open your books | The action comes first, the noun is one she can picture, and it works with a finger or a mouse. |
| REWRITE | strings.js > pupil > coverSwitch | Not you? Switch account | A verbless question at the front door, and she cannot tell whether the whole line or only the last two words is the button — use: “Use a different account” |
| REWRITE | strings.js > pupil > coverFirstTime | First time here? Google will ask your permission once, then you are straight in. | “First time here?” has no verb, “ask your permission” never says permission for what, “straight in” is an idiom, and the two halves are joined by a comma splice — use: “The first time you sign in, Google will ask if you allow MathShelf to know your name. Say yes once, and you will not be asked again.” |
| REWRITE | strings.js > pupil > coverNoServer | We could not reach the server. Check your connection and reload the page. | “The server” is a thing she has never seen, and “check your connection” asks a girl on a school Chromebook to fix something she cannot touch — use: “MathShelf could not load your books just now. Wait a moment, then reload this page. If it happens again, tell your teacher.” |
| REWRITE | strings.js > pupil > coverWrongClass | That class link is not active. Ask your teacher for the link again. | “Is not active” is machine language for a state she did not cause — use: “This link no longer opens your class. Ask your teacher to send you the link again.” |
| PASS | strings.js > pupil > coverNameLabel | Your name | A two-word label on the box it belongs to, and she knows exactly what goes in. |
| REWRITE | strings.js > pupil > coverNamePrompt | We could not read your name from your account. Write it once and it is saved. | “Write it once and it is saved” has to be read twice, and the app says type everywhere else but write here — use: “We could not read your name from your account. Type your name in the box — you only need to do this once.” |
| REWRITE | strings.js > pupil > coverNameMissing | Write your name first, so your teacher sees it on her class list. | “Her class list” tells a girl whose teacher is a man that the sentence is not about him — use: “Type your name first, so your teacher can see it on the class list.” |
| REWRITE | strings.js > pupil > coverPreview | This is the preview copy of MathShelf. Nothing here is saved to school. | “The preview copy” is build language and “saved to school” is not a phrase she can picture — use: “This is a practice copy of MathShelf. Nothing you do here is sent to your teacher.” |
| PASS | strings.js > pupil > shelfGreetingMorning | Good morning | Two ordinary words, exactly as a person would say them. |
| PASS | strings.js > pupil > shelfGreetingAfternoon | Good afternoon | Two ordinary words, exactly as a person would say them. |
| PASS | strings.js > pupil > shelfPlain | Your maths books live here. Your teacher chooses which ones are out. | It names the shelf, says whose books they are and who puts them out, and reads clean aloud in one pass. |
| REWRITE | strings.js > pupil > shelfNotSet | Not set yet | “Set” has two meanings on a shelf of books, and the note beside it explains it with a different word again (“put out”) — use: “Not out yet” |
| REWRITE | strings.js > pupil > shelfNotSetNote | A book that is not set yet is one your teacher has not put out for this class. | A definition folded back on itself that she has to unpick before it means anything — use: “Your teacher has not put this book out for your class yet.” |
| PASS | strings.js > pupil > shelfMore | Your teacher will add more books to this shelf during the year. | One idea, concrete nouns, and it answers the question a half-empty shelf would raise. |
| REWRITE | strings.js > pupil > shelfMarks | {got} of {max} marks · {done} of 8 answered | The second half drops its noun, so she has to work out what the 8 is counting — use: “{got} of {max} marks · {done} of 8 questions answered”; this render also still shows {got} and {max} instead of her numbers, which is worth one check. |
| REWRITE | strings.js > pupil > backToShelf | The shelf | A place name where an action belongs, so she has to guess that pressing it takes her back — use: “Back to the shelf” |
| PASS | strings.js > pupil > opening | Opening Angles… | It names the book she just chose, so she knows her press worked. |
| PASS | strings.js > pupil > nudgeBanner | Your teacher suggested watching this method. | It says who sent it and what it is, in her words, and she can act on it from where she is sitting. |
| REWRITE | strings.js > pupil > selfEvalSaved | Saved. Your teacher sees this on her class list. | “Her class list” assumes the teacher is a woman, and “sees this” promises a look the screen cannot promise — use: “Saved. Your teacher can see this on the class list.” |
| PASS | strings.js > pupil > selfEvalHeading | Exercise finished — how did that go? | It reads exactly as a person would say it, and she can tell she is being asked, not marked. |
| REWRITE | strings.js > pupil > selfEvalOptional | optional | One long word that never says what is optional — use: “You can skip this.” |
| PASS | strings.js > pupil > saveWaiting | Still saving your work… | It names her work, says the app is still busy, and asks nothing of her. |
| PASS | strings.js > pupil > saveRetry | Try again | Two words, one action, and she knows what pressing it does. |
| REWRITE | strings.js > pupil > saveHeldLocal | Your work is safe on this device and will be sent as soon as the page can reach the server. | Twenty words ending on “the server”, a thing she has never seen, with the page as the one doing the reaching — use: “Your work is safe on this device. Nothing has been lost, and it will be sent on as soon as it can.” |
| PASS | strings.js > pupil > movieWorkedExample | Worked example | Classroom furniture she hears from her teacher every week, used as the heading of the thing itself. |
| PASS | strings.js > pupil > moviePrevStep | Previous step | It names the step it moves to, and works with a finger or a mouse. |
| PASS | strings.js > pupil > movieNextStep | Next step | It names the step it moves to, and works with a finger or a mouse. |
| PASS | strings.js > pupil > moviePlayLabel | Play | One word she has pressed on every video she has ever watched. |
| PASS | strings.js > pupil > moviePlayBtn | ▶ Play | The glyph and the word say the same thing, so neither has to be decoded alone. |
| PASS | strings.js > pupil > moviePauseBtn | ❚❚ Pause | The word carries the meaning even if the two bars do not. |
| PASS | strings.js > pupil > movieReplayBtn | ↺ Replay | The word carries the meaning even if the arrow does not, and it tells her the film can be watched again. |
| REWRITE | strings.js > pupil > classifyYourAnswer | You answered “{answer}” — your teacher can see this and will pick it up in class. | “Pick it up in class” is an idiom and a promise the screen cannot keep, and she is left not knowing whether she was right — use: “You answered “{answer}”. Your teacher will go through this one with you in class.” |
| PASS | strings.js > pupil > protractorCheckBtn | Mark my measurement | It matches the Mark my … family she meets on every other question, and it names what is being marked. |
| REWRITE | strings.js > pupil > protractorInstructions | Put the small crosshair at the centre of the protractor on the corner. Turn the protractor with a rotate knob at either end, until 0 sits along one arm, then read where the other arm crosses. | Three steps run together as prose, with a preposition pile-up (“at the centre of the protractor on the corner”) that garbles which thing goes where — use a numbered list: “1. Put the crosshair in the middle of the protractor on the corner of the angle. 2. Turn the protractor with a knob at either end, until the 0 line sits along one arm. 3. Read the number where the other arm crosses the scale.” |
| PASS | strings.js > pupil > protractorReadTrueWrongScale | You read the other scale — use the one that starts at 0 on the arm you lined up. The true size is 65°. | It names the slip she actually made, gives the fix in her own words, and only then gives the true size. |
| REWRITE | strings.js > pupil > protractorReadTrueGeneric | Not quite — line the centre on the corner and 0 along an arm, then read again. The true size is 65°. | “Line the centre on the corner” is missing its “up with”, and telling her to read again after handing her the answer contradicts itself — use: “Not quite — the true size is 65°. Line the centre of the protractor up with the corner, and 0 along one arm, then read the other arm.” |
| REWRITE | strings.js > pupil > protractorScaleRetry | Close — but check you are reading the scale that starts at 0 on your lined-up arm. One more go. | “Your lined-up arm” is a squashed compound she stumbles over, and “One more go” never says what to press — use: “Close. Check you are reading the scale that starts at 0 on the arm you lined up, then try once more.” |
| REWRITE | strings.js > pupil > protractorLineUpRetry | Line it up carefully and read again — one more attempt. | “Line it up” gives “it” no name anywhere on that screen, and the sentence ends in a fragment — use: “Line the protractor up carefully and read it again. You have one more try.” |
| REWRITE | strings.js > pupil > protractorTypeFirst | Type the size you measured first. | “The size you measured first” reads two ways and she has to choose one — use: “First type in the size you measured.” |
| PASS | strings.js > pupil > protractorMaxRange | A protractor measures up to 180° — read the size again. | It gives her the fact she was missing and sends her back to the scale without saying what the answer is. |
| PASS | strings.js > pupil > protractorMeasureLabel | Your measurement in degrees | It names the box and the units, so nothing has to be guessed. |
| PASS | strings.js > pupil > protractorMeasurePlaceholder | the size you measure, in degrees | It says what goes in the box without seeding a number that would do the reading for her. |
| PASS | strings.js > pupil > measuredReadout | · you measured 65° | Short and factual, and it shows her back the number she typed. |
| REWRITE | strings.js > pupil > checkLockedWhy | This one is finished. | “This one” leaves her to work out what is finished and whether she can still change it — use: “You have already finished this question.” |
| PASS | strings.js > pupil > checkNeedsLineWhy | Write a line of working first. | The action comes first, and a line of working is the thing the page has already asked her for. |
| REWRITE | strings.js > pupil > checkNeedsAngleWhy | Work out the angle you were asked for first. | “The angle you were asked for first” reads two ways, and the passive makes her hunt for who asked — use: “First work out the angle the question asks for.” |
| REWRITE | strings.js > pupil > addedToJotter | ✓ Added to your jotter. | There is no jotter on her screen — the thing she is writing on is the page — use: “✓ Added to your page.” |
| PASS | strings.js > pupil > algebraNextLine | Write your next line of working: | It names the action and the thing, and it sits above the box she types in. |
| PASS | strings.js > pupil > removeLastLineBtn | ↶ remove last line | It names exactly what will disappear, so she can undo without fear. |
| PASS | strings.js > pupil > removedLastLine | Removed your last line. | It confirms what just happened in four words she can read at a glance. |
| PASS | strings.js > pupil > lineTooLong | That line is too long for the page — split it into two steps. | It says what is wrong and what to do about it, and splitting into two steps is a fix she can carry out. |
| REWRITE | strings.js > pupil > lineUnreadable | That line does not read as maths yet — check it and try again. ({reason}) | “Does not read as maths yet” is a curious phrase, “check it” never says check what, and the bracketed reason on the end is machine output on a pupil screen — use: “That line does not look like maths yet. Check your signs and brackets, then try again.” |
| REWRITE | strings.js > pupil > pageFull | That is a full page — press Mark my working. | “That is a full page” is the bare demonstrative-plus-is shape that reads badly aloud — use: “Your page is full — press Mark my working.” |
| REWRITE | strings.js > pupil > secondAttemptNote | Second attempt — your first try stays on the page. | It calls the same thing an attempt and then a try in one line, so she stops to check whether they are two different things — use: “This is your second try — your first one stays on the page.” |
| REWRITE | strings.js > pupil > algebraLinePlaceholder | your next line, then “add line” | A noun phrase and a button name joined by a comma, with no verb anywhere — use: “type your next line here”, and put “then press add line” above the box. |
| PASS | strings.js > pupil > addLineBtn | add line | Two words, one action, and it matches the words used about it elsewhere. |
| REWRITE | strings.js > pupil > moveAnnotationEquation | What are you doing to both sides? — tag the move (optional) | The question is the right one for an equation, but “tag the move (optional)” is platform jargon she has never met — use: “What are you doing to both sides? Choose the step you took — you can skip this.” |
| REWRITE | strings.js > pupil > moveAnnotationGeneric | What’s your next step? — tag the move (optional) | The same jargon tail: “tag the move (optional)” means nothing from her seat — use: “What is your next step? Choose the step you took — you can skip this.” |
| PASS | strings.js > pupil > chipCollectTerms | Collect terms | The department's own words for the move, and she has met them in class and in the film. |
| REWRITE | strings.js > pupil > chipJustRewrite | Just rewrite | It reads like an instruction to rewrite rather than a description of what she did, so she cannot tell what pressing it claims — use: “Just rewrote it” |
| REWRITE | strings.js > pupil > moveOperandLabel | How much? | “How much?” never says how much of what — use: “How much? Type the number or term you used.” |
| REWRITE | strings.js > pupil > moveOperandPlaceholder | how much? e.g. 15 or 3x | “e.g.” is an abbreviation she has to decode, and it repeats the question the label above already asked — use: “for example, 15 or 3x” |
| PASS | strings.js > pupil > moveOperandNext | next → | One word and an arrow, both pointing the same way. |
| REWRITE | strings.js > pupil > substIntro | Choose each letter below to put its number in. | “To put its number in” never says where the number goes — use: “Choose each letter in turn, and put in the number you were given for it.” |
| PASS | strings.js > pupil > substGivenLabel | given: | The standard label she meets in her own exercise book, with the numbers beside it doing the explaining. |
| REWRITE | strings.js > pupil > substAnswerPrompt | Now work it out, then enter the value: | “The value” is a word she has to translate into “the answer” before she can act — use: “Now work it out, then type your answer:” |
| REWRITE | strings.js > pupil > expandIntro | Multiply every term — choose the right product for each box. | “The right product” asks her to hold a term the film may never have said in plain words — use: “Multiply every term — choose the right answer for each box.” |
| REWRITE | strings.js > pupil > solveChooseMove | Choose a move — you will see the next line, already checked for balance. | “already checked for balance” hangs off a comma with no verb, and “balance” is a word she has not been given — use: “Choose what to do to both sides. The next line is written out for you, with both sides still equal.” |
| REWRITE | strings.js > pupil > solveNeedNumber | Enter a number for that move first. | “That move” points at a platform word, and the trailing “first” makes her read the sentence twice — use: “First type the number you want to use.” |
| REWRITE | strings.js > pupil > angleOneOpenHint | One angle is dashed on the diagram — choose it, give its size, and choose the reason. | Three actions strung on commas where a numbered list is wanted, and “give its size” never says to type it — use: “1. Choose the angle marked with dashes. 2. Type its size. 3. Choose the reason.” |
| REWRITE | strings.js > pupil > angleMultiOpenHint | Choose a dashed angle on the diagram, give its size and reason, then work your way to {target}. | Three actions on commas again, ending in the idiom “work your way to” — use: “1. Choose an angle marked with dashes. 2. Type its size and choose the reason. 3. Keep going until you have {target}.” |
| PASS | strings.js > pupil > removeLastStepBtn | ↶ remove last step | It names exactly what will disappear, so she can undo without fear. |
| PASS | strings.js > pupil > removedLastStep | Removed your last step. | It confirms what just happened in four words she can read at a glance. |
| REWRITE | strings.js > pupil > angleStepHeading | Work out ∠Aoife | The ∠ symbol is a code she has to decode, and the label directly under it spells the word out instead — use: “Work out angle Aoife” |
| PASS | strings.js > pupil > angleSizeLabel | Size of angle Aoife | It spells out “angle” and names the box, so nothing has to be decoded. |
| FAIL | strings.js > pupil > angleUnreadable | That does not read as a number yet — type a number, or a calculation like 180−65. | It hands her a worked calculation built from the question's own numbers, which is the banned “e.g. 180−38” leak telling her to subtract from 180; delete the example so the line ends “— type the size in degrees.” |
| REWRITE | strings.js > pupil > nudgeBannerStar | ★ Your teacher suggested watching this method. | The star means an earned award on her shelf, so putting it on a teacher's nudge makes her read the nudge as praise — use: “Your teacher suggested watching this method.” |
| REWRITE | strings.js > pupil > selfEvalSavedFlash | ✓ Saved — your teacher sees this on her class list. | “Her class list” assumes the teacher is a woman — use: “✓ Saved — your teacher can see this on the class list.” |
| REWRITE | strings.js > pupil > selfEvalSavedIdle | Saved as you go — your teacher sees this on her class list. | “Saved as you go” is loose, and “her class list” assumes the teacher is a woman — use: “Your answers save by themselves — your teacher can see them on the class list.” |
| FAIL | strings.js > pupil > angleUnreadableLine | That does not read as a number yet — type a number, or a calculation like 180−65. | The same leak as the other unreadable-number message: 180−65 tells her the method and uses the question's own numbers; delete the example so the line ends “— type the size in degrees.” |
| PASS | strings.js > teacher > passcodeChecking | Checking the passcode… | From a teacher's seat it says what is happening and asks nothing of her. |
| PASS | strings.js > teacher > openMarkbook | Open the markbook | A teacher sees the action and the thing it opens, named as the markbook is named everywhere else. |
| PASS | strings.js > teacher > noServer | We could not reach the server. Try again. | For a teacher between lessons it says what failed and what to do, in eight words. |
| PASS | strings.js > teacher > serverNotInitialised | This markbook has not been set up yet. Open Set-up and add a class first. | A teacher is told the state and sent to the named screen, Set-up, with the first thing to do there. |
| PASS | strings.js > teacher > serverNotSignedIn | Google did not say who you are. Reload the page and sign in again. | A teacher can act on this at once, and the two steps are in the order she takes them. |
| PASS | strings.js > teacher > serverBadName | That name could not be saved. Letters and spaces only. | A teacher is told what failed and what the rule is, with nothing to decode. |
| REWRITE | strings.js > teacher > serverUnknownClass | There is no class with that code. Check the link, or the code on the board. | A teacher who has never opened this markbook does not know which board “the code on the board” means — use: “There is no class with that code. Check the link, or the class code you gave the pupils.” |
| PASS | strings.js > teacher > serverNotSet | That book is not switched on for this class. Tick it on in Set-up. | A teacher gets the state and the fix, and “tick it on” matches the words used inside Set-up. |
| PASS | strings.js > teacher > serverNotYourClass | That class was made by another teacher, so it is not yours to change. | It tells a teacher why the control will not act, in one plain sentence, beside the control. |
| REWRITE | strings.js > teacher > serverExists | There is already a class with that name. | It tells a teacher what is wrong but not what to do next, which costs her the time the markbook is meant to save — use: “There is already a class with that name. Choose a different name.” |
| REWRITE | strings.js > teacher > serverNotFound | That could not be found. Reload the page and try again. | “That” leaves a teacher not knowing what could not be found — use: “That class or book could not be found. Reload the page and try again.” |
| REWRITE | strings.js > teacher > serverUnreachableStore | The app cannot reach its own store, so nothing was saved and nothing was lost. Tell Damien the front door is not joined to the data deployment. | “Its own store” and “the front door is not joined to the data deployment” are build words on a maths colleague's screen — use: “MathShelf cannot reach the place where work is saved, so nothing was saved and nothing was lost. Tell Damien that the markbook cannot reach its saved data.” |
| REWRITE | strings.js > teacher > serverTooBig | There is more working in this book than one save can carry. Tell Damien before the class writes any more. | It alarms a teacher without telling her whether anything is lost, and “one save can carry” is internal — use: “This book holds too much working to save in one go, so the newest work may not be saved. Tell Damien before the class writes any more.” |
| REWRITE | strings.js > teacher > serverUnknownAction | The app asked for something this server does not know about. Reload the page. | A teacher can do nothing with the diagnosis and is not told whether to report it — use: “Something went wrong between the page and the markbook. Reload the page, and tell Damien if it happens again.” |
| PASS | strings.js > teacher > signedInAs | Signed in as aoife.gartland@c2ken.net | A teacher can check at a glance that she is in her own account. |
| PASS | strings.js > teacher > loadingClass | Loading 10A-Maths… | A teacher sees which class is opening, so a slow load is never a mystery. |
| PASS | strings.js > teacher > readingBooks | Reading {done} of 8 books… | A teacher gets a count that moves, so she knows the wait has an end. |
| PASS | strings.js > teacher > relockedLeft | The markbook closed when you left it. Enter the passcode to open it again. | A teacher is told why the markbook is shut and exactly how to open it again. |
| PASS | strings.js > teacher > relockedIdle | The markbook closed itself after fifteen minutes. Enter the passcode to open it again. | A teacher is told the reason, the time it took, and the way back in. |
| PASS | strings.js > teacher > inkChange | Tap or click a mark to change it. | It names both ways in, which the smartboard and the laptop each need, and it says what the press does. |
| PASS | strings.js > teacher > inkMine | Mark it right | Three words, one action, in a teacher's own language. |
| PASS | strings.js > teacher > inkMineWrong | Mark it wrong | Three words, one action, in a teacher's own language. |
| PASS | strings.js > teacher > inkUseApp | Use the app’s mark | A teacher can see this hands the mark back to the app, which the note above it has already explained. |
| PASS | strings.js > teacher > needsYouLabel | Needs you now | A teacher reads the heading and the list of pupils under it together, and the meaning arrives with them. |
| PASS | strings.js > teacher > needsYouWrongTwice | Wrong twice on Angles, {exercise}, {question} | A teacher gets the pupil's trouble and its exact place in one line she can scan between lessons. |
| REWRITE | strings.js > teacher > needsYouPulledHelp | Used the method help on Angles, {exercise}, {question} and is still wrong | It calls the film “the method help” where the button beside it calls it the worked example, so a teacher meets two names for one thing — use: “Watched the worked example on Angles, {exercise}, {question}, and is still getting it wrong.” |
| PASS | strings.js > teacher > needsYouStuck | Nothing saved for {minutes} minutes on Angles, {exercise}, {question} | It reports only what the markbook actually knows — nothing saved, for how long, and where — which a teacher can act on. |
| PASS | strings.js > teacher > csvCopied | Copied. Paste it into a spreadsheet. | A teacher is told the copy worked and where to put it. |
| PASS | strings.js > teacher > csvFallback | Your browser would not let the page copy for you. Select the text below and copy it yourself. | A teacher is told why the button did nothing and what to do instead, in her own words. |
| PASS | strings.js > teacher > setUpHint | Tick the books this class should see. | The action comes first, and it matches the wording used when a book is not switched on. |
| REWRITE | strings.js > teacher > overConfident | Over-confident — confidence high, working weaker | It labels the child rather than the work, and hangs two verbless scraps off a comma, on a screen a teacher may have on the board — use: “Rated this work higher than it scored” |
| REWRITE | strings.js > teacher > quietlyExcelling | Quietly excelling — doing well, low confidence | The same shape and the same problem: a personality verdict on a child where a description of the work belongs — use: “Rated this work lower than it scored” |
| PASS | strings.js > teacher > cellWrongAtStep | Wrong at step 2 | A teacher gets the place in the working in plain words, with no codes. |
| PASS | strings.js > teacher > cellWrongFirstSlip | Wrong — first slip at step 2 | A teacher reads “first slip at step 2” as ordinary English and can open the book knowing where to look. |
| PASS | strings.js > teacher > markedAria | Marked right | The spoken label says exactly what the mark is. |
| REWRITE | strings.js > teacher > reteachTitle | Sends this exercise’s worked example (Angles) to this pupil | The bracketed book name leaves a teacher unsure whether Angles is the book or the exercise, and the sentence has lost its subject — use: “Sends this pupil the worked example for this exercise in Angles.” |
| PASS | strings.js > teacher > closeMarkbook | Close the markbook | A teacher sees the action and the thing, and knows what she is closing. |
| REWRITE | strings.js > teacher > whereYouAre | Where you are | A heading that never says where in what, on a screen a teacher has never opened before — use: “The class and book you are looking at” |
| PASS | strings.js > teacher > whichBook | Which book | A teacher reads it as the label of the book chooser it sits on. |
| PASS | strings.js > teacher > needsYouAria | Pupils who need you now | The spoken label glosses the panel's heading in full. |
| PASS | strings.js > teacher > nobodyStuck | Nobody is stuck in Angles right now. | A teacher gets a clear all-clear, and it names the book it is about. |
| PASS | strings.js > teacher > copyByHand | Copy this by hand: ols.link/10a | A teacher at a smartboard is told exactly what to write up, and the address is short enough to write. |
| REWRITE | strings.js > teacher > couldNotSave | We could not save that. | “That” never says what failed, and a teacher is given nothing to do next — use: “That change could not be saved. Try it again.” |
| PASS | strings.js > teacher > deleteClassAria | Delete 10A-Maths | The spoken label names the class that would go, so nothing is deleted blind. |
| PASS | strings.js > teacher > loadingGrid | Loading the full grid… | A teacher waits knowing what is coming, and the line under it explains what the grid is. |
| REWRITE | strings.js > teacher > gridOrient | Each cell is one pupil and one question. Look for the reds, then open a cell to read that pupil's book. | “Look for the reds” asks a teacher to read a colour with no key beside it, and it never says how to open a cell — use: “Each cell is one pupil and one question. Tap or click a cell to read that pupil's book; the red cells are the ones marked wrong.” |
| PASS | strings.js > teacher > cellNotStarted | Not started | Two words a teacher reads without stopping. |
| FAIL | strings.js > teacher > cellUntouched | Not started | It prints the same two words as the other not-started state, so a teacher cannot tell the two apart on the grid; the two states need different words, and only you know which distinction is meant. |
| REWRITE | strings.js > teacher > cellRight | Right — working and answer both sound | “Both sound” makes a teacher pause on a word with another meaning — use: “Right — working and answer both correct” |
| REWRITE | strings.js > teacher > cellAmber | Answer only, no working shown | It is the approved line with its dash swapped for a comma, which joins two verbless halves — use: “Answer only — no working shown.” |
| PASS | strings.js > teacher > fetchingBook | Fetching this pupil's book… | A teacher knows whose book is coming and that the wait is hers to expect. |
| PASS | strings.js > teacher > pencilPosture | These marks are the app's. Tap or click any mark to change it to yours — yours is the one that counts. | It names both ways in, glosses whose mark is whose, and ends on the thing a teacher most needs to know. |
| REWRITE | strings.js > teacher > reteachBtn | Show them this method again → | “Them” for one named pupil reads as plural, and “this method” is a third name for the worked example — use: “Send this pupil the worked example again →” |
| REWRITE | strings.js > teacher > reteachSent | Sent ✓ — this pupil sees it next time | “Next time” never says next time what, so a teacher is left wondering when it lands — use: “Sent ✓ — this pupil will see it next time the book is opened” |
| REWRITE | strings.js > teacher > reteachFailed | We could not send that. | “That” never says what failed, and a teacher is given nothing to do next — use: “The worked example could not be sent. Try again.” |
| PASS | strings.js > teacher > saveFailedMark | We could not save that — choose the mark again to try once more. | A teacher is told what failed and given the exact press that retries it. |
| PASS | strings.js > teacher > readingEveryBook | Reading every book… | A teacher knows what the wait is for. |
| REWRITE | strings.js > teacher > starterBtn | Next-lesson starter ▶ | A teacher cannot tell what pressing it will produce — use: “Make a starter for the next lesson ▶” |
| REWRITE | strings.js > teacher > csvCopiedFull | CSV copied — paste it straight into a spreadsheet. | CSV is an abbreviation, and the other copy message on this same markbook manages without it — use: “Copied — paste it straight into a spreadsheet.” |
| PASS | strings.js > teacher > seriesKs3 | KS3 · M2 | KS3 and the CCEA unit code are a maths colleague's daily currency, like the department's own WALT. |
| PASS | strings.js > teacher > seriesGcse | GCSE · M3 & M4 | GCSE and the CCEA unit codes are a maths colleague's daily currency, like the department's own WALT. |
| REWRITE | strings.js > pupil > classifyTryAgain | Not that one — look again at its size against 90° and 180°. | Carried line: “Not that one” opens on a bare demonstrative, and 90° and 180° are handed to her as numbers instead of the right angle and straight line she has actually met — use: “Not right — compare the angle with a right angle and with a straight line, then choose again.” |
| FAIL | strings.js > pupil > angleSizePlaceholder | the size, e.g. 65 | Carried line: a worked example sitting inside the answer box, and as rendered the example number is the answer to the very angle on this screen; delete it and label the box “the size in degrees”. |
