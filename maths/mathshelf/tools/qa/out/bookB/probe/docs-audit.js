const fs=require("fs");let s=fs.readFileSync("tools/qa/MATHS_GATES_AUDIT.md","utf8");
const anchor="### After the steward re-cut of Exercise 3's chart — 11 Sept 2026, 19:47–20:40 (sit-pupil only)";
if(!s.includes(anchor)) throw new Error("anchor");
s=s.replace(anchor, `### Book A's battery, as it ran — 13 Sept 2026, 00:34 → ~01:20, in the background (tools/qa/out/bookA/control-all.log)
The whole battery, pooled, on a tree that was NOT the deployed one for most of its run: Book B's cut opened at 00:35 and its packages were editing statcore.js, jotter-stats.js, strings.js, style.css, the walker and the new pack under it. Read accordingly. **Every pure gate's control FIRED** — qa-two-homes 15/15 (the split's \`acts-hardcoded\`, \`acts-any-string\`, \`active-spreadsheet-call-site\` included), qa-waits 10/10, qa-language 4, qa-coverage 6, qa-manifest 3, qa-colour-law's \`amber-outside-a-mark\` (Book A's new plant) FIRED, qa-click-safety \`stats-a-single-press-lift\` FIRED, and the film-law plants \`film-no-table\`, \`film-no-venn\`, \`film-no-vfill\`, \`film-ring-draws-nothing\`, \`film-box-on-the-glyphs\` FIRED. **The walker controls are void for this run**: sit-pupil, sit-confused and sit-teacher each report "over-tightening RED — the shipped tree fails its own gate" (the shipped tree at that moment carried a half-built Book B), so their 36 "DID NOT FIRE — the gate failed, but not with …" rows (the twelve \`film-no-*\` of Book A and the earlier ops, the three \`stats-a-*\`, the polish-cut controls, \`stats-a-snap-back\`) prove nothing either way and are re-run after Book B's deploy on the deployed tree. Genuinely not firing, unchanged from the standing table above: \`extract-transcript\` \`no-walk-no-transcript\`, \`qa-fonts\` \`face-removed\`.

### Book B's controls — 13 Sept 2026 (declared; the battery runs after the deploy, in the background)
New single-fault controls, each quoting the sentence its gate prints: \`sit-pupil\` \`stats-b-list-fig-missing\` (a values question's printed list not on the page), \`stats-b-stage-skipped\` (a table jumps empty → ready), \`stats-b-rowpick-two-pressed\` (two rows pressed in one ask — the new \`ROWPICK_SINGLE\` probe), \`stats-b-total-before-cells\`, \`stats-b-tfn-leak\` (a verdict word on the page before Check — the \`SIGNATURE\` probe widened to \`.stat-verdict-leak\`); the sixth plant, \`stats-b-cell-inconsistent\` (a wrong fx marked right by the consistency rule), is written but UNREGISTERED — it needs a bespoke wrong attempt, and the engine's own selfTest covers the rule (\`markTable\` cases in dev/test-statcore.js). The lint's Book B rules are proved on two throwaway packs: \`tools/qa/out/bookB/lint-probe-pass.js\` (7 questions, clean) and \`lint-probe-fail.js\` (30 questions, one fault each → 43 attributed failures; the sentences are in LINT_NOTES.md). Every walker control is \`controls: pending the post-deploy battery\` until that run's FIRED lines are pasted here.

` + anchor);
fs.writeFileSync("tools/qa/MATHS_GATES_AUDIT.md",s);
