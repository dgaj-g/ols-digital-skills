## 13 SEPT 2026, 00:35 → — BOOK B, Handling Data · Averages (clock stamped 00:35 BST at `b2e5be9`, budget 180 min; RESUME_AB, Opus 5 orchestrating four sonnet/opus packages by file ownership)

**BOOK B IS LIVE — FRONT DOOR Version __VB__, 13 Sept 2026 __TB__, from `__CB__`; a CLIENT-ONLY cut (server/Code.gs md5 unchanged from Version 34, `3e3f4819…`), so the store stays the standalone project's Version 1 and no manifest was touched. The Sheet's Config row `acts` now reads `["stats-collect","stats-averages"]` (typed by the session; `qa-tickbox` prints that exact line). Book B arrives UNTICKED for every class. Proved at `7175f3d` (the deploy commit): `--fast` green; `--full --book stats-averages` GREEN on every walker (sit-pupil 430 ×3 widths, sit-confused 178 ×3, sit-teacher 31/31/52), every Book B cell closed; the whole-tree `--full` GREEN except the rows this entry names below; the separated cold read filed against the final hashes — Book B __CR_B__, the shell __CR_V4__ (the standing declined rows stand: statStageIqr, statStageCfplotPlace, the KS3·M2 codes). Post-deploy `qa-manifest` __QM__, `qa-repo-prod` __QRP__. The control battery runs in the BACKGROUND after this (`tools/qa/out/bookB/control-all.log`) and is reported in the audit when it ends.**

**TIME, against the 180-minute budget, line by line (wall-clock from the 00:35 stamp):** packages spawned 00:36–00:52 (ENGINE-B opus, CONTENT-B, LINT-B, WALK-B sonnet) · my renderer (`BUILD.table`, the set of five, the judge's table/list, strings, CSS, wiring) 00:45–01:10 · the pack installed and the fast tier's reds (notation, lexicon, define-before-use, near-duplicates) back to CONTENT-B and green 01:00–01:15 · the walk's own findings fixed 01:25–01:45 (the `bracket` op that does not exist → `ring`; the s4 rings that rang the wrong line → written and ringed; the blank corner `<th>`; the no-name-cell law re-scoped to the table kind after it condemned Book C) · `--full --book` 01:29–01:37 · the judge 01:40–02:10 and its 13 REWRITEs honoured 02:05–02:12 · pre-flight whole-tree `--full` 01:42–02:08 · the final whole-tree `--full` at `7175f3d` 02:13–__TF__ · the cut __TB__. **Total __TOTAL__ of 180.**

**What is NOT proved and is his:** the eight-item smoke list plus Book B's own line (fill one f × x cell wrong on purpose, put the right total under it, and see the total marked with the hollow tick — "the total of your own column"); the shelf showing Book B the moment a class is ticked; a real pupil account (the deployer's visit proves the chain, never the pupil path).

**Left for the next cut, dated 13 Sept 2026:** the walker controls `stats-b-*` (five registered, one — `cell-inconsistent` — written but unregistered: it needs a bespoke wrong attempt) are declared and the battery to prove them runs after this deploy; a `bracket {i,j}` film op ("circle the middle two", an even-count median) does not exist in player.js — Book B's s1 film rings the one middle value of its nine, and the contract's line was corrected; the s5 reserve "which average makes her statement right" judge and the two reverse-mean reserve grids the closed FT table cannot express (CONTENT_NOTES.md); the judge's standing shell REWRITEs (pageFull/angleUnreadable "That", "Show them", "Tick it on in Set-up"); Book A's battery of 00:34 ran on a tree mid-Book-B and its walker rows are void (audit).

**BOOK B.** One new kind in `jotter-stats.js` — `table` (the given columns printed, every derived cell a box the pad fills top to bottom then the totals row, the questions under the table last: a number box for the mean, a row of pressable class names for the modal class and the class the median is in; stages `empty · filling · asking · ready`, pills "Fill the table" · "The answers"; a wide table scrolls in its own frame and says so, and at ≤640 the cells tighten so a four-column grouped table fits a phone) — plus `values` with a printed list (`fig:{type:'list'}`), a given table (`fig:{type:'table'}`) and a set of five boxes (`set:5`, the constraints slot), and `judge` drawing the list or table its claims are about above the claims with a stage line that says "choose one of the answers" when the options are not changes. Engine (`statcore.js`, ENGINE-B): `tableDerive`, `markTable` (a cell right when consistent with HER row, a total right when the sum of HER column, the mean right when `sum(fx)/sum(f)` of HER table; dp rounding as half a unit; dx `AV_DIV_ROWS`, `AV_FX_NOT_SUMMED`, `AV_NO_MIDPOINT`, `AV_MEDIAN_CLASS_OFF`), the six list/reverse-mean slips in `valuesDx` (`AV_MEDIAN_UNORDERED`, `AV_MODE_AS_FREQ`, `AV_RANGE_NOT_DIFF`, `AV_DIV_ROWS`, `RM_AVERAGED_MEANS`, `RM_WRONG_N`) sharing one `slipValue` with the wrong model board, `rm.newMean` with `xFrom`, `set:5` read before the blank test (a real bug); selfTest **270 → 360**. Lint (LINT-B): the table's structure and every re-derivation (cells, totals, the mean to dp exact-or-rounded, median/mode asks by id, modal and median rows by the engine's own rule), the list stats re-derived from the printed list with every slip proved distinguishable (`dx:false` waives one — q4's range, whose list has a 0), `set:5` satisfiability by brute force, and eight judge proof kinds (`estMeanDivisor`, `countAtLeast`, `medianInterval`, `modalInterval`, `exactFromGrouped` by two consistent datasets, `costOf`, `changes` — now proved for Change/Stay-the-same claims too — `compareAverage`); 43 attributed failures on a 30-fault probe pack. The pack (CONTENT-B): **5 sections, 29 questions (16 values, 9 table, 4 judge), 40 film steps, 84 marks; Core = 22 units of 26**; every answer re-derived by hand and by the lint from SOURCE_INVENTORY 1.4/1.9 (the booklet's images 3–27 read from the .docx media); Alma's seedlings' "range is 25" NEI proof corrected by the lint (uniform classes make the naive bounds coincide). Walkers (WALK-B): `pressTable`, the set-of-five route, `table` detection and sentences, `TABLE_ONE_CELL`/`ROWPICK_SINGLE` probes, the list-figure law, six `stats-b-*` plants (five registered), the transcript's `head` sample.

**Faults the walk and the judge found and fixed before anything shipped:** the film op `bracket` named in my contract does not exist (the s1 median rings one value); the s4 film's two `ring` ops rang numbers on the mean line, not the table rows (each now a written line — "Modal class: 27 aircraft in 0–10" — with its value ringed); the s5 advantages table's blank corner `<th>` ("Average"); the no-name empty-cell law WALK-B wrote condemned Book C's compare/boxplot boxes on the whole-tree walk (scoped to `.stat-tablekind`); the judge's 13 REWRITEs — a verbless comment-bank line, "group"/"class interval"/"class" for one idea across Ex 4 (one word now: class), seven bare option fragments on the Changes/Stays-the-same claims (now sentence endings "…the mean weight will…" · Change / Stay the same), a bare "That" in the s5 film. Faults of the pack the fast tier found: ASCII ranges in prose, "girl," to the lexicon, a dash-chain, two vocab entries pointing at captions spoken too late, three near-duplicate labels.
